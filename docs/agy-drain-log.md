# agy drain log (append-only; installer-excluded)

## drain 2026-07-22 — manual EXTEND drain (41 installed-inbox observations -> GROWTH)

Run BY HAND, not via `just drain-knowledge`: that automation is stale relative to the shipped SEED+GROWTH
audience split — it targets the four driver manuals + `seed/golden-header.md` with zero `growth`/`curate-commit`
awareness, so running it would misroute every observation into driver-owned manuals and never touch the runtime
GROWTH. See the "update drain tooling to EXTEND" task. Owner chose the manual EXTEND path.

GROWTH regenerated wholesale via `clavity-ls curate-commit` (STDIN): 9720 B; SEED+GROWTH = 11787 B < 16 KB cap.
Written byte-identical to the compiled draft, 0 mojibake (U+0393 count 0), 29 em-dashes intact, `.sha256` sidecar
valid. The installed `clavity-ls` carries the T2 CP437-decode fix, so no re-corruption on write.

Triage of the 41 (all project-agnostic; NO backlog-eligible driver/deterministic tool-fixable entry — the only
mitigation for every deterministic entry is a driving move, so none became a fix-the-tool item):

- PROMOTED (folded/consolidated into GROWTH at strongest phrasing) — the reinforced 9 prior patterns plus new
  clusters: send-PATHS-not-conclusions / a prose-only consult verifies nothing (>=3 cross-session observations);
  review-only "banner governs intent, the payload BODY governs inference" + snapshot-VCS-before/after-any-peer-task
  + the-peer-fabricates-an-OWNER-DECISION; confabulation refinements (revision-pin stale reads, verify the
  RELATION not just the endpoints, version-stale token, long-review decay, an unquoted concession is unresolved);
  oversized-turn hard-transport-ERROR mode + the "NO PRIOR RESULT" escape hatch + hard-char-budget-in-the-original;
  panel refinements (real-but-inflated-severity, false-reason-right-remedy, rejected-findings-are-worth-measuring,
  a bare clean verdict needs per-lens evidence); high-value review LENSES to seed (guard-fires-on-proxy-not-the-
  invariant, make an oracle choice name its false-pass AND false-fail, pre-state the privilege/trust boundary);
  process-environment-mechanics as a peer STRENGTH + pin-a-local-probe-to-the-real-call's-lifecycle-stage;
  name-the-specialist-skill to engage the expert mindset for a review/design ask.
- PARKED (Empirical Assumption -> `docs/agy-verify-needed.md`, needs a live probe): the priming hypothesis.
- DROPPED outright: none. Every observation was routed and considered; single-observation refinements were
  CONSOLIDATED into the pattern they strengthen (the "reinforce" disposition), not promoted as standalone rules.
- Driver cheatsheet `driver-cheatsheet.core.md`: UNCHANGED — the four meta-bullets already carry the distilled
  essence; the new material is refinement-detail routed to GROWTH, so the byte-identical baseline-floor pins in
  both binaries are untouched and no cheatsheet oracle was needed.

Raw 41 snapshotted before draining. Installed inbox `## Pending` reset to empty. NOTE: the repo source inbox
(`agy-autotrain/knowledge/agy-observations.md`, 2 entries) DIVERGES from the canonical installed inbox (41) —
a pre-existing drift, not reconciled by this manual drain.
