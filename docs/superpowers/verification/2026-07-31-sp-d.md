# SP-D (degradation guards, tests, anti-drift) — verification transcript

**Verified against commit:** 6e380f0
**Method:** ONE non-adversarial pass. NOT a capstone.
**Ordering note:** run before this plan's Task 6, which modifies agy-liveness-check.sh.

**Step 0 correction note:** this plan's Step 0 assertion 4 originally stated
`clavity-dotnet/plugin/hooks/agy-liveness-check.sh` is 86 lines. A first pass through this task measured it
at 85 lines and stopped with `STATE_MISMATCH`. The coordinator re-checked and confirmed the file is
unchanged since commit `26cd99a` — 85 lines / 4760 bytes — and that the plan's line-count figure was simply
wrong when written (the byte count in the same table was correct). The plan was corrected and committed as
`6e380f0`. This pass re-measured the file (below, Step 2) and confirms: **85 lines**, and the EXIT-CODE
CONTRACT comment block is present at lines 12-19 exactly as the plan describes. The discrepancy is resolved,
not ignored.

## Deliverables asserted

| Deliverable | Plan reference | Command run | Result |
|---|---|---|---|
| Liveness hook shipped, byte-identical across both plugins, documented exit contract at lines 12-19 | Step 0 assertion 4 / Step 2 | `diff -q clavity-dotnet/plugin/hooks/agy-liveness-check.sh clavity-classic/plugin/hooks/agy-liveness-check.sh && echo IDENTICAL`; `sed -n '12,19p' clavity-dotnet/plugin/hooks/agy-liveness-check.sh` | PASS — `IDENTICAL`, exit=0; EXIT-CODE CONTRACT block present at 12-19; see §Step 2 |
| jq guard retrofit on agy-after-reminder | Step 3 | `grep -n 'command -v jq' clavity-dotnet/plugin/hooks/agy-after-reminder.sh` | PASS — 1 match, line 17, exit=0; see §Step 3 |
| Hook-activation test matrix green | Step 4 | `just test-scripts` | PASS — `Tests Passed: 321, Failed: 0, Skipped: 0, Inconclusive: 0, NotRun: 0`, exit=0, 23 `.Tests.ps1` files exercised; see §Step 4 |

## Step 2 — liveness hook byte-identity + exit contract, verbatim output

```
$ diff -q clavity-dotnet/plugin/hooks/agy-liveness-check.sh clavity-classic/plugin/hooks/agy-liveness-check.sh && echo IDENTICAL
IDENTICAL
exit=0
```

```
$ sed -n '12,19p' clavity-dotnet/plugin/hooks/agy-liveness-check.sh
# EXIT-CODE CONTRACT: (1) healthy / superpowers live -> exit 0, no stderr; (2) a DETECTION OUTCOME warranting
# a notice (superpowers not-live incl. corrupt/unreadable settings, .no-agy active, or jq missing) -> the line
# on stderr + exit 2. Every reachable end-state is one of those two explicit exits; there is NO code path
# OUTSIDE detection. `set +e` is the fail-open guard: a mid-detection command failure does not abort the hook,
# it continues to the not-live advisory (exit 2) -- non-blocking for SessionStart and fail-toward-loud, the
# posture Decision 3 wants (a soft advisory beats a silent swallow for a liveness hook). NO blanket
# `trap ... ERR` -- it would swallow the settings-parse path and drop the advisory. The ONLY silent outcome is (1).
# Byte-identical across both driver plugins (kept honest by the seed-sync gate).
```

Both files are byte-identical (`diff -q` printed `IDENTICAL`, exit 0). Separately confirmed via `wc -l`:
both `clavity-dotnet/plugin/hooks/agy-liveness-check.sh` and `clavity-classic/plugin/hooks/agy-liveness-check.sh`
are **85 lines** (not 86 — see the Step 0 correction note above). The EXIT-CODE CONTRACT comment block is
present verbatim at lines 12-19, matching the plan's description. Matched.

## Step 3 — jq guard retrofit, verbatim output

```
$ grep -n 'command -v jq' clavity-dotnet/plugin/hooks/agy-after-reminder.sh
1 matches in 1 files:

clavity-dotnet/plugin/hooks/agy-after-reminder.sh:17:if ! command -v jq >/dev/null 2>&1; then
exit=0
```

(The `1 matches in 1 files:` prefix line is this environment's `rtk` shell-command proxy formatting the
`grep -n` invocation, not custom scaffolding added to the command — the underlying match is the single line
shown: `agy-after-reminder.sh:17`.) One match, at line 17, `grep` exit 0. Matched — the jq guard is present.

## Step 4 — hook-activation test matrix, verbatim output

Run in the FOREGROUND with the 600000ms (10-minute) timeout, not backgrounded. It completed inside that
budget. Stdout exceeded inline capture size and was persisted to a file by the harness; that file was read
in full and its line count confirmed explicitly with `wc -l` (473 lines) rather than assumed from what was
visible on screen.

```
$ just test-scripts; echo "exit=$?"
pwsh -c "Invoke-Pester scripts/tests -Output Detailed -CI"
Pester v5.8.0

Starting discovery in 23 files.
Discovery found 321 tests in 3.61s.
Running tests.

Running tests from 'C:\Users\user\Development\Rust\clavity\scripts\tests\abort-drain.Tests.ps1'
...
```

The run covers all 23 files under `scripts/tests/`, confirmed independently by counting the file list:

```
$ ls scripts/tests/*.Tests.ps1 | wc -l
23
```

This matches Pester's own "Starting discovery in 23 files" line and the 23 `Running tests from '...'` headers
counted in the captured output (`grep -c "Running tests from" <capture> = 23`).

The two files most directly scoped to this task's assertions — `agy-liveness-check.Tests.ps1` and
`agy-after-reminder.Tests.ps1` — in full, nothing elided:

```
Running tests from 'C:\Users\user\Development\Rust\clavity\scripts\tests\agy-after-reminder.Tests.ps1'
Describing agy-after-reminder.sh
  [+] fires the AGY-AFTER reminder on a spec write 1.62s (1.61s|3ms)
  [+] fires on a plan write 1.38s (1.37s|13ms)
  [+] is silent on a non-artifact path 1.24s (1.24s|2ms)
  [+] is suppressed by .no-agy in cwd 1.51s (1.51s|1ms)
  [+] is suppressed by a global $HOME/.claude/.no-agy 848ms (847ms|2ms)
  [+] emits a LOUD jq-missing line on a spec path when jq is absent 562ms (560ms|2ms)
  [+] is silent (no jq-missing line) on a non-artifact path when jq is absent 513ms (512ms|1ms)
  [+] ships as pure ASCII 87ms (86ms|2ms)

Running tests from 'C:\Users\user\Development\Rust\clavity\scripts\tests\agy-liveness-check.Tests.ps1'
Describing agy-liveness-check.sh
  [+] is SILENT (exit 0, no stderr) when superpowers is enabled 907ms (904ms|3ms)
  [+] ADVISES (stderr + exit 2) when superpowers is disabled 1.3s (1.3s|2ms)
  [+] ADVISES when no settings files exist at all 815ms (813ms|2ms)
  [+] honors the settings hierarchy: project-local disable overrides user enable -> ADVISES 908ms (907ms|1ms)
  [+] honors the hierarchy: project-local enable overrides user disable -> SILENT 875ms (874ms|1ms)
  [+] ADVISES on a corrupt (unreadable) settings.json (never fail-open) 852ms (851ms|1ms)
  [+] announces .no-agy in cwd (exit 2) and does NOT also emit the superpowers/jq notice 1.77s (1.77s|2ms)
  [+] announces a global $HOME/.claude/.no-agy 816ms (814ms|1ms)
  [+] emits ONE jq-missing warning (exit 2) when jq is absent 399ms (398ms|1ms)
  [+] ships as pure ASCII 107ms (106ms|1ms)
```

All 8 tests in `agy-after-reminder.Tests.ps1` and all 10 tests in `agy-liveness-check.Tests.ps1` passed
(`[+]`, no `[-]` failures). The remaining 21 files' output (abort-drain, accept-drain, agy-seam-inject,
agy-test-audit-reminder, BashHookHelpers, check-agy-discipline-skills, check-core-integrity,
check-growth-budget, check-member-docs, check-plugin-namespace, check-roster, check-seed-artifacts-synced,
check-seed-budget, check-user-facing-docs, compute-release, docs-audit, drain-knowledge, drain-lib,
generate-scoped-manifest, register-plugin, release-lib) is omitted from this transcript as out of scope for
this task's SP-D assertions — they exercise other tools' gates, not the SP-D deliverables; the full 473-line
capture was read in full to confirm this and that none of those blocks contained a `[-]` failure marker.

Final summary lines (verbatim, from the same captured output, tail of the run):

```
Tests completed in 446.2s
Tests Passed: 321, Failed: 0, Skipped: 0, Inconclusive: 0, NotRun: 0
exit=0
```

All 321 tests across all 23 files passed. `exit=0`, matching the expected result.

## Step 5 — the epic's unmet definition of done

SP-D's design requires "ONE combined release closes the epic". Measured:

```
$ git for-each-ref --sort=-creatordate --format='%(refname:short) %(creatordate:short)' refs/tags | head -3
clavity-v13 2026-07-24
clavity-v12 2026-07-21
clavity-v11 2026-07-20
```

```
$ git log --all -i --grep='sp-d' --format='%h %cs' | head -3
529e769 2026-07-31
97f8d53 2026-07-31
ac964e3 2026-07-25
```

`--grep='sp-d'` is a plain text search over commit messages — it finds commits whose message happens to
contain the string "sp-d" (case-insensitive), which is not a reliable enumeration of SP-D's actual work. It
returns **17 commits total** (counted explicitly with `wc -l`, not by eyeballing a capped list — the `head -3`
above intentionally shows only the newest 3; all 17, with full ISO timestamps, are below):

```
$ git log --all -i --grep='sp-d' --format='%h %cI %s'
529e769 2026-07-31T23:53:26+03:00 docs(plan): productize completion audit imp...
97f8d53 2026-07-31T22:38:51+03:00 docs(spec): productize completion audit des...
ac964e3 2026-07-25T17:12:12+03:00 docs(sp-d): superpowers-prerequisite note +...
b5873ee 2026-07-25T17:04:16+03:00 test(sp-d): SP-0 namespace-gate regression ...
f6e95ee 2026-07-25T16:57:53+03:00 test(sp-d): pin Git Bash + isolate HOME on ...
2a44935 2026-07-25T16:50:09+03:00 feat(sp-d): register liveness hook in both ...
9dc2f8c 2026-07-25T16:38:20+03:00 feat(sp-d): SessionStart liveness/degradati...
7ae1fb6 2026-07-25T16:21:03+03:00 feat(sp-d): jq-guard retrofit on agy-after-...
81b5fcf 2026-07-25T16:10:59+03:00 fix(sp-d plan): correct CLAUDE_CONFIG_DIR e...
ad22673 2026-07-25T16:10:55+03:00 test(sp-d): shared bash-hook Pester helper ...
56997cb 2026-07-25T15:49:58+03:00 fix(sp-d): seed-sync selector retains group...
fa63c7a 2026-07-25T15:34:51+03:00 docs(sp-d): fold panel findings - exit-code...
630a9f8 2026-07-25T15:22:29+03:00 docs(sp-d): implementation plan (line-verif...
68b694f 2026-07-25T12:26:19+03:00 docs(sp-d): fix emission mechanism + exit-c...
3ec357f 2026-07-25T12:01:18+03:00 docs(sp-d): harden spec with consuming-agen...
3820a5a 2026-07-25T11:51:19+03:00 docs(sp-d): design spec - degradation guard...
3f31d85 2026-07-24T14:43:41+03:00 docs(spec): ship-agy-workflow design — 4 di...
```

Two of the 17 (`529e769`, `97f8d53`, both 2026-07-31) are this-repo's productize-audit spec/plan work, not
SP-D implementation — they matched only because "sp-d" appears as a label inside that audit doc. The oldest
hit, `3f31d85` (2026-07-24), is the umbrella 4-discipline design spec, not an SP-D-specific commit either.
The actual `feat(sp-d)`/`fix(sp-d)`/`test(sp-d)`/`docs(sp-d)`-prefixed implementation commits run from
`3820a5a` (2026-07-25T11:51:19+03:00) through `ac964e3` (2026-07-25T17:12:12+03:00).

Checked whether the newest tag genuinely predates that work, rather than assuming it:

```
$ git rev-parse clavity-v13
b31591600132630fb8a986df7a0258b6d52e4cef
$ git log -1 --format='%H %cI' clavity-v13
b31591600132630fb8a986df7a0258b6d52e4cef 2026-07-24T09:32:22+03:00
$ git merge-base --is-ancestor clavity-v13 3f31d85 && echo YES
YES
```

`clavity-v13` was cut **2026-07-24T09:32:22+03:00**. The earliest SP-D-related commit found by the grep
(`3f31d85`, the umbrella spec) lands **2026-07-24T14:43:41+03:00**, about 5 hours later the same day, and is
a git descendant of the tag (`merge-base --is-ancestor` confirmed `YES`). The actual SP-D implementation
commits (`3820a5a` through `ac964e3`) all land the following day, 2026-07-25. `clavity-v13` remains the
newest tag in the repository (confirmed above — no tag postdates it). So the claim holds, verified directly
rather than assumed: **the newest release tag (`clavity-v13`, 2026-07-24T09:32:22+03:00) predates every
SP-D-related commit measured (earliest 2026-07-24T14:43:41+03:00, the bulk of the implementation
2026-07-25), and no tag has been cut since** — no release contains this work. The disciplines have never
shipped. This is not a defect in SP-D's artifacts — they are present (Steps 2-4 above) and their tests pass
— it is the epic's closing step, addressed by this plan's Task 8.

## Notes

This transcript does not fix or adjust any epic artifact, including `agy-liveness-check.sh`, which a later
task in this plan (Task 6) modifies — its state here is the pre-Task-6 shipped baseline. All steps above are
pure assertion built from direct measurement, not from the plan's pre-written prose.
