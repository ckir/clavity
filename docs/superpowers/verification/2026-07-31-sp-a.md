# SP-A (agy-first + agy-negotiate) — verification transcript

**Verified against commit:** d3f1f49
**Method:** ONE non-adversarial pass. Read the plan's deliverables, assert each exists and matches.
This is NOT a capstone.

## Deliverables asserted

| Deliverable | Plan reference | Command run | Result |
|---|---|---|---|
| agy-first skill byte-identical across both plugins | SP-A required a byte-identical mirror | `diff -q clavity-dotnet/plugin/skills/agy-first/SKILL.md clavity-classic/plugin/skills/agy-first/SKILL.md && echo IDENTICAL` | PASS — `IDENTICAL`, exit=0; see §Step 2 |
| Marker-contract doc names the agy-first discipline | `docs/agy-disciplines-marker-contract.md` | `grep -n 'agy-first\|AGY-FIRST' docs/agy-disciplines-marker-contract.md \| head` | PASS — 4 matches; see §Step 3 |
| agy-first enrolled in seed-sync artifact list | `scripts/check-seed-artifacts-synced.sh` | `grep -n 'agy-first' scripts/check-seed-artifacts-synced.sh` | PASS — 1 match (line 17); see §Step 4 |
| `just seed-sync-check` exits 0 | seed-sync gate | `just seed-sync-check; echo "exit=$?"` | PASS — exit=0; see §Step 4 |
| `just check-agy-skills` exits 0 | agy-discipline skill lint gate | `just check-agy-skills; echo "exit=$?"` | PASS — exit=0; see §Step 4 |

## Step 2 — agy-first skill byte-identity, verbatim output

```
$ diff -q clavity-dotnet/plugin/skills/agy-first/SKILL.md clavity-classic/plugin/skills/agy-first/SKILL.md && echo IDENTICAL
IDENTICAL
exit=0
```

Expected `IDENTICAL` — matched.

## Step 3 — marker-contract doc, verbatim output

```
$ grep -n 'agy-first\|AGY-FIRST' docs/agy-disciplines-marker-contract.md | head
12:  - `<discipline>` ∈ { `agy-first` (SP-A), `agy-capstone` (SP-B), `agy-test-audit` (AGY-TEST-AUDIT) }.
41:   P produces the SAME filename as S (`clavity-agy-first.head`), adding fragile parsing for an identical
43:2. **P regresses on migration.** Retaining one `agy-first.head` across a classic->dotnet swap is the
56:  - `agy-first` writes after a consult completes (ALIGNED / REJECTED / resolved NEGOTIATE).
exit=0
```

4 matches — the doc explicitly names `agy-first` as the SP-A discipline (line 12) and documents its
marker-write behaviour (line 56). Matched.

## Step 4 — seed-sync enrollment + gate runs, verbatim output

```
$ grep -n 'agy-first' scripts/check-seed-artifacts-synced.sh
17:  skills/agy-first/SKILL.md \
exit=0
```

The skill appears in the sync list (one line, path entry `skills/agy-first/SKILL.md`).

```
$ just seed-sync-check; echo "exit=$?"
bash scripts/check-seed-artifacts-synced.sh
seed agent artifacts in sync (dotnet == classic)
exit=0
```

```
$ just check-agy-skills; echo "exit=$?"
pwsh -NoProfile -Command "./scripts/check-agy-discipline-skills.ps1"
agy-discipline skills OK
exit=0
```

Both recipes exited 0. Expected — matched.

## Capstone evidence

`git log --oneline --all -i --grep=capstone` returns 50 commits total (verbatim output below). Of
those, the commits scoped to SP-A / agy-first are:

- `58607ee fix(agy-first): harden lint against frontmatter body-smuggle + empty-file crash (capstone R1)`
  — an actual AGY-CAPSTONE round-1 fold, verified by reading the full commit message (2 findings folded,
  measurement-verified, 5 pinning tests).
- `e66bc85 docs(plan): SP-A agy-first discipline plan (panel-reviewed, pre-SP-0-rebase)` — matches the
  grep only because its body mentions the *skill name* `agy-capstone` in passing; it is a plan-writing
  commit, not a capstone fold, and does not count as a fold commit.

So there is exactly **one** SP-A-scoped fold commit (`58607ee`), confirming the plan's claim. A single
fold round with no recorded GREEN round is not a reconstructible convergence. This transcript is the
evidence that the SHIPPED ARTIFACTS match the plan; it makes no claim that an adversarial capstone
converged. Per the source spec, SP-A does not get a capstone-ledger row.

### Verbatim `git log --oneline --all -i --grep=capstone` output

```
1b75c34 docs(spec): fold panel round 5 (3 findings, all self-inflicted by ear...
283253d docs(spec): fold panel rounds 2 and 3 (8 more findings)
97f8d53 docs(spec): productize completion audit design (panel round 1 folded)
fbb126b test(verify): check every tool the hook needs, not just jq and awk
200c3ff test(verify): exercise the no-driver fallback instead of skipping it
20834b0 docs(verify): record the gate's real limit -- it cannot see a deleted...
a52ef9d fix(classic): resolve psmux lazily so importing the module needs no b...
8fcbfa6 fix(verify): close three silent-failure paths the capstone found
fd816b9 verify(agy-autotrain): re-run the probe harness on agy 1.1.9 - A4 and...
8d00a56 fix(clavity-ls): require adjacent ports on the id-fallback pairing path
813a861 docs(clavity-ls): record why DiscoverActive validates only the newest...
18b2421 docs(clavity-ls): correct the 3rd glog field claim and the stale pair...
5542d38 fix(clavity-ls): make the session-id match a preference, and never ab...
3d42ae7 fix(clavity-ls): pair the HTTP line of the SAME agy session, and cove...
31ddba2 fix(clavity-ls): surface an unusable captured number as LsDiscoveryEx...
884c2d4 docs(roadmap): add spending-audit brainstorming task — round count, c...
ab8dc3a docs(roadmap): add AGY-SCOPE brainstorming task — pre-existing defect...
00b0e6d fix(installer): correct the partial-failure warning + scoped not-dete...
cd1a209 fix(agy-test-audit): capstone R3 folds - core.quotePath=false for non...
be2a5e3 fix(agy-test-audit): capstone R2 folds - case-insensitive ext grep (F...
61bb193 fix(agy-test-audit): capstone R1 folds - no-jq .no-agy payload-cwd (F...
0756bbe docs(agy-test-audit): marker contract - audit marker + capstone-gate ...
4f089a8 feat(agy-test-audit): marker-gated capstone->audit reminder hook + tests
db4e0fd docs(adversarial-panel-review): verify the peer's FIX, not just its F...
17d75b8 docs(agy-test-audit): fold AGY-AFTER panel rounds 1-2 (11 + 6 finding...
82338d6 chore(agy-autotrain): capture 2 agy observations (correct-finding-def...
08abc67 fix(clavity-ls): clear sawChannelDeath on a successful reach so a sta...
f9acb60 docs(agy-test-audit): design spec for the AGY-TEST-EXHAUSTIVENESS-AUD...
f2bab54 fix(clavity-ls): report channel_down (not waiting_for_human) when the...
131591e fix(clavity-ls): propagate a caller-cancel from the three boot/model ...
89dbcbe docs(sp-c): auto-fire hook design spec (AGY-AFTER panel-GREEN)
0f5e3a1 fix(sp-b): fold agy-capstone R4 (mid-adjudication sha-race + shell-ag...
a879cce fix(sp-b): fold agy-capstone R3 - close breach-waiver gate-bypass
98ffcbd fix(sp-b): fold agy-capstone R2 findings (honest best-effort envelope...
2c105ac fix(sp-b): fold agy-capstone R1 findings (3, measurement-verified)
d2ce314 docs(sp-b): document agy-capstone terminal-state write trigger + WAIV...
4f9fc71 feat(sp-b): enroll agy-capstone/SKILL.md in the seed byte-identity gate
30df137 test(sp-b): stage agy-capstone in every lint rejection fixture + pin ...
8ca0204 feat(sp-b): agy-capstone discipline skill (both plugins) + lint enrol...
799fab3 docs(sp-b): agy-capstone implementation plan (AGY-AFTER panel-GREEN)
4ba0114 docs(sp-b): AGY-CAPSTONE skill design — panel-hardened to GREEN
58607ee fix(agy-first): harden lint against frontmatter body-smuggle + empty-...
e88853a fix(sp0): CRLF-harden responder frontmatter strip (AGY-CAPSTONE R2)
9b53a48 fix(sp0): harden SP-0 gates per AGY-CAPSTONE round 1 (4 findings)
e66bc85 docs(plan): SP-A agy-first discipline plan (panel-reviewed, pre-SP-0-...
3f31d85 docs(spec): ship-agy-workflow design — 4 disciplines to users (panel-...
2ee376d fix(docs-audit): guard 0-byte oracle + correct injection-order commen...
7e27eab docs(accuracy): fix the offboarding twin of the check-versions step
6708857 docs(accuracy): fix a third "CI runs exactly these" and a stale onboa...
097db87 docs(accuracy): drop two more false "pre-push runs the tests" claims
```

(50 lines total.)

## Notes

This transcript does not fix or adjust any epic artifact. All steps are pure assertion; every command
run exited 0 and every expected result matched.
