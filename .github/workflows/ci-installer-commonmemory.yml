name: ci-installer-commonmemory

# Continuous version-consistency coverage for the commonmemory member (plugin-only). Inno retired
# 2026-09-14: build-commonmemory.yml is now validate-only (plugin.json version truth, no installer), so
# this no longer watches installer/_shared/** or generate-scoped-manifest.ps1 — neither ships this member.
# The workflow file keeps its ci-installer- name to preserve any required-check reference.
on:
  push:
    branches: [main]
    paths:
      - 'commonmemory/**'
      - '.github/workflows/build-commonmemory.yml'
      - '.github/workflows/ci-installer-commonmemory.yml'
      - 'scripts/check-versions.ps1'
  pull_request:
    paths:
      - 'commonmemory/**'
      - '.github/workflows/build-commonmemory.yml'
      - '.github/workflows/ci-installer-commonmemory.yml'
      - 'scripts/check-versions.ps1'

concurrency:
  group: ci-installer-commonmemory-${{ github.ref }}
  cancel-in-progress: true

jobs:
  installer:
    uses: ./.github/workflows/build-commonmemory.yml
