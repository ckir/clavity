//! xtask — the plugin packager (STUB / skeleton).
//! Usage (planned): cargo xtask package <plugin> [--mode universal|split]

mod gen;
mod pack;

fn main() {
    // TODO(first-plugin): parse args, call pack::package().
    eprintln!("xtask: stub — packager not yet implemented");
}
