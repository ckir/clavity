# agy capability profile — what agy can do, and how to route to it

> **Verified against: agy 1.0.8** · models per the `/model` menu (Axis C). Provenance-tagged
> (`[corpus]`/`[doc]`/`[local]`/`[bus]`/`[verified]`/`[conflict]`). Companion to the **empirical**
> [`agy-assumptions.md`](agy-assumptions.md) (what's verified here + how to re-verify) and the
> **how-to-ask** [`agy-remote-control-protocol.md`](agy-remote-control-protocol.md). The user's
> auto-consult policy (when to ask agy before showing the user) is the `agy-first-brainstorm.sh` hook.
>
> **agy is a dynamic, multi-model platform, not a static model:** effective capability =
> *the active model + agy's orchestration + native tools + currently-loaded skills & MCP servers*.
> Check what's active/loaded (model via `/model`, MCP via `/mcp`, skills) — the profile below is the
> baseline; the live config is the truth.

<!-- Axes filled during synthesis (plan Task 5) from docs/agy-capabilities-research.md. -->

## A. Strengths (route toward)

## B. Weaknesses & failure modes (route away / guardrail)

## C. Reasoning profile & model selection

## D. Operational reach

## E. Control surface that changes capability

## F. Routing: whether agy, and on which model

## G. Version & drift

## Refresh after an `agy update`
