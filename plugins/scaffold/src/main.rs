//! scaffold-server — the scaffold plugin's MCP server (STUB / skeleton).
//! Real "hello" tool implementation lands with the first plugin.

fn main() {
    // TODO(first-plugin): build mcp_core::Server with a `hello` tool and run() it.
    eprintln!("scaffold-server: stub — not yet implemented");
}
