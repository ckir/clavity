# AGY-CAPSTONE ledger

One row per capstone. Appended before a plan may be declared complete.

**This is a RECORD, not a proof.** Nothing prevents someone appending a GREEN line without running
anything; a self-asserted ledger is the same shape as the re-stamping defect the verify gate removed.
Two things keep it honest, neither a guarantee: the `evidence` column must cite something independently
checkable, and the ledger is reviewed like any other artifact rather than trusted like a gate output.

**`none` is not a permitted evidence value.** A capstone that goes green on its first round still
produces a transcript — the rounds it ran, the lenses it seated, what it tried. Cite that. If there is
nothing to cite, the entry does not go in.

**A row is written BEFORE adjudication and must be RETURNED TO after it.** The verdict a capstone
proposes is `GREEN (proposed)`; only the owner turns that into `GREEN`. Nothing prompts that second edit,
and on 2026-08-06 a row was found still claiming `(proposed)` long after its marker had been written —
the record was pessimistic rather than wrong, but a cell nobody trusts to be current is a cell nobody
reads. **When the owner confirms, come back and say so, and cite the REVIEWED tip** (which is not the
commit that added the ledger row — that commit is by construction unreviewed).

**How to tell a confirmed GREEN from a waiver, later, without asking anyone.** Both write the marker, so
the marker alone does not distinguish them. A round-cap waiver ALSO appends a `WAIVED` line to
`.clavity/agy-marks/skipped.log`; a confirmed GREEN appends nothing.

🔴 **MATCH THE LINE TO THE HEAD, NOT TO THE FILE.** That log is APPEND-ONLY ACROSS EVERY CAPSTONE, so
"absence of a line there" is never true after the first waiver and was the wrong test to write down. On
2026-08-28 it already held three `WAIVED` lines from 2026-08-24, -25 and -26, none of them belonging to
the capstone being adjudicated - and the driver, checking the file rather than the range, briefly recorded
"no WAIVED line exists". Each line carries `HEAD=<sha>`, so the check is: **is there a `WAIVED` line whose
HEAD is inside the range this row claims?** Absence of THAT, plus a written marker, is the confirmation. Note that log is gitignored runtime state, so this check works on the
machine that ran the capstone and nowhere else — if you need the fact to survive the machine, put it in
the row.

**Absences are meaningful.** SP-0, SP-A, SP-C and SP-D do not appear below. They never had a
reconstructible capstone; their evidence is a verification transcript under
`docs/superpowers/verification/`, which is a different and weaker claim, deliberately not laundered into
this table.

| date | range | rounds | verdict | evidence |
|------|-------|--------|---------|----------|
| 2026-07-25 | SP-B agy-capstone skill | 4 | GREEN | folds 2c105ac, 98ffcbd, a879cce, 0f5e3a1 |
| 2026-07-27 | agy-test-audit discipline | 3 | GREEN | folds 61bb193, be2a5e3, cd1a209 |
| 2026-07-30 | clavity-ls channel resilience | 3 | GREEN | folds 131591e, f2bab54, 08abc67 |
| 2026-07-31 | b14bef1..fbb126b | 5 | GREEN | folds 8fcbfa6, a52ef9d, 20834b0, 200c3ff, fbb126b |
| 2026-08-08 | `bd2ddb7..97f012d` (ROADMAP §11 PINNING-ASSERTION-STRENGTH — the peer-free assertion-strength hook) | 2 | **GREEN — owner-confirmed 2026-08-08.** Marker `.clavity/agy-marks/agy-capstone.head` = `97f012d`, the REVIEWED tip — deliberately NOT ambient HEAD, which is the ledger-row commit and is unreviewed by construction (a review cannot review its own record). No `WAIVED` line in `skipped.log` for this HEAD, so this is a clean adjudicated GREEN, not a waiver. No review-only breach in either round: tree clean, HEAD and reflog tip unmoved both times. | fold `97f012d`; briefs `.clavity/seams/assertion-strength-capstone{,-r2}.md` (one per round). **R1: 7 seats (Axiom Breaker, Cascade Analyst, State Corruptor, Boundary Smuggler, Protocol Pedant, Resource Vampire, Mechanism Gamer), ALL “no new findings”. R2: 4 FRESH seats (Activation Auditor, Blindspot Auditor, Dependency Cynic + bespoke Fold Consistency Auditor), also all clean.** Anchors correct in both rounds — **including the file line-count, so the long-standing EOF actual+1 quirk did NOT fire**; citations `:143`/`:30` spot-checked verbatim. 🔴 **THE RUN'S ONE FINDING WAS THE DRIVER'S, NOT THE PEER'S, and it was a total-inertness risk:** the hook was registered as its OWN `PostToolUse` group whose `Write|Edit` matcher overlaps `agy-after-reminder`'s, and whether a runtime dispatches EVERY matching group or only the FIRST could not be settled by measurement — if the first, the feature ships *installed, registered, inert and invisible*, the §0 failure mode. Evidence was suggestive but NOT decisive (the other overlapping group also matches `Bash`, so it proves nothing about Write; `docs/plugin-formats.md:23` gives the shape, not the dispatch semantics; and `agy-capstone-ledger.md:48` records an earlier capstone logging an `Agent|Task` matcher question UNVERIFIED for the same reason). **Owner ruled: co-locate** both commands in the one group. 🔴 **THE FIX WAS THEN VERIFIED, NOT ASSUMED — and R2's peer stand-down had rested on an UNCITED claim that “Claude Code dispatches commands within a group independently”, the very class of unmeasured assumption that caused the finding.** Measured instead: the `SessionStart: startup|resume|clear|compact` group already ships **3 commands in one group**, predates this work, and was OBSERVED firing at this session's start (three separate SessionStart notices arrived together). So within-group multi-command dispatch has live in-repo precedent where multi-group-same-matcher had none — the fold moved the assumption onto solid ground rather than sideways. Pinned by a 2-row co-location test (both drivers), proven non-vacuous by restoring the separate-group shape: **exactly the dotnet row went red, 36/1**, classic correctly staying green because only dotnet was mutated. Suites: `test-scripts-fast` **404/0** (was 369/0; delta = the new suite), assertion-strength **37/0** on the reviewed tip. **Both Task-5 mutants attributed per-row** (predicate → 19/16 every FIRE red and every SILENT green; debounce → 31/4 hitting exactly the 3 debounce rows). ⚠ **Two test defects were caught during execution by the plan's own stop conditions, not by any panel:** the suite passed with NO HOOK ON DISK (bash's *No such file or directory* error contains the hook FILENAME, and a bare `Should -Match 'ASSERTION-STRENGTH'` matched that — 13 green against an empty repo, incl. all 5 FIRES rows), and the tests isolated `$HOME` but not `$TMPDIR` so they were never isolated at all (the hook prefers `${TMPDIR:-/tmp}`) — 7 spurious failures. |
| 2026-08-01 | 185affc..757337a | 3 | GREEN | folds f8d9703, 01622ce, 757337a; briefs .clavity/seams/phase-b-capstone-r{1,2,3}.md |
| 2026-08-01 | 19f589a..18495cd | 3 | GREEN | folds da18681, 18495cd; briefs .clavity/seams/anomaly-capstone-r{1,2,3}.md |
| 2026-08-02 | 29a5db8..c70f145 | 4 | GREEN | folds d089552, 0ca7407, c70f145; briefs .clavity/seams/capstone-anomaly-fix-r{1,2,3,4}.md |
| 2026-08-02 | 6d79bee..a0b2d7b (AT-2 durability) | 3 | GREEN | folds 3adc045, a0b2d7b; round 1's miss reproduced by the probe recorded in 3adc045's message |
| 2026-08-03 | 4a25d7a..5dc8822 (curate-nudge age scan) | 4 | GREEN | folds a357d7a, 8099813, 5dc8822; brief `.clavity/seams/capstone-nudge.md` (rewritten per round). Every finding measured before folding; round 3's finding was one the peer itself dismissed as unrealistic |
| 2026-08-06 | a605275..413c617 (pre-release defect sweep, `.no-agy` repo-root walk) | 2 | GREEN (owner-confirmed 2026-08-06; reviewed tip `be851cc`) | fold 413c617; **LIVE-PROVEN in the INSTALLED plugin**, which is the evidence that matters most here: the same smoke (`.clavity/scratch/prerelease/livesmoke.ps1`, takes `-HooksDir`) run against the pre-fix installed hooks kept as a backup scored **9/9 BROKEN** — every hook fired straight through a root `.no-agy` — against **9/9 correct with every control firing** after the hot-copy. Same script, same payloads, same machine; only the hooks differed; briefs `.clavity/seams/prerelease-capstone-r{1,2}.md` + the fork brief `prerelease-unc-fork.md`. **R1 GREEN rejected as false, the 5th consecutive one** — it cited `agy-consult-guard.Tests.ps1:489-505` (file has 146 lines) and `agy-test-audit-reminder.sh:113` (has 104), and reported *measured* file counts of 28/14 that are really 25/11. Numbered "quote line N verbatim, or reply DOES NOT EXIST" made it retract both citations and name the cause: round-1 line numbers were **diff-stream offsets, not source lines**. **The defect that mattered was found by the DRIVER running the shipped hook against its own pre-change self** — an unreachable UNC cwd cost 20343ms against 3882ms before, worst on the two PreToolUse hooks. **R2 (Regression Auditor, rotated in) was well-grounded and produced one real new finding** — extended-length UNC (`//?/UNC/server/share/...`) walks two levels past the true volume root — **verified, then measured at 974ms because the `[ -d ]` gate short-circuits that shape entirely. Below the severity floor; recorded, not fixed.** The peer's own suggested fix was WRONG on its number (claimed the gate costs one stat / ~3882ms; measured 9481ms) while its refutation of the obvious `//`-prefix alternative was RIGHT and changed what shipped: WSL repos are live UNC paths |
| 2026-08-07 | `cdd0de0..a99e242` (plan 2: direct-driver anomaly capture on `UserPromptSubmit` + the inbox-snapshot slash-command path) | 2 | GREEN (owner-confirmed 2026-08-07; reviewed tip `a99e242`) | Briefs `.clavity/seams/plan2-capstone.md` (rewritten per round). Suites at the reviewed tip: **fast 347/0 (273.3s), slow 262/0 (700.9s with a genuine `Tests completed` line)**. Every file in the range is `M` — none added — so the `justfile` registration list needed no change, verified rather than assumed. Before/after snapshots clean: HEAD *and* reflog tip unchanged, no review-only breach. **NO code fold was needed; the value of this capstone was two peer claims killed by measurement.** R1 (6 seats) and R2 (4 fully rotated seats) both returned all-seats-GREEN; **both were probed, and both broke** — the ninth and tenth times in this epic that a probed clean verdict did. (1) R1's *"both emission paths are character-for-character byte-identical"* is **FALSE**: Windows `jq` terminates with CRLF, the hand-built `printf` with LF (611 vs 610 bytes on the new arm). It **predates this range** (600 vs 599 on the old `PreCompact` arm), and the functional invariant holds — but it exposed a real inaccuracy: the hook comment at `agy-anomaly-capture-reminder.sh:29-30` claims a test pins BYTE identity, while `agy-anomaly-capture-reminder.Tests.ps1:138` compares `.systemMessage` *after* `ConvertFrom-Json`, pinning the MESSAGE. **Captured as tracked debt, deliberately not fixed** — pre-existing, and a fix would touch both driver hooks and move the reviewed tip. (2) R2's *"a tab separator diverges between the jq and grep paths"* is **HALF REFUTED**: measured with a firing space-separator control, a tab fails on **both** paths identically — the fallback greps the RAW payload, where a tab is the two characters `\t` and a backslash is not `[[:space:]]`. The non-trigger is real but **below the reachability floor**, and the four `case` patterns are a plan-specified complete set; **disposed, not folded.** Right about the behaviour, wrong about the mechanism — in the same answer |
| 2026-08-03 | c7b3923..8889473 (agy discipline cost/quota hygiene) | 4 | **GREEN — owner-confirmed.** Marker `8889473` (the REVIEWED tip, not the ledger commit `8d2daa3`). Cell corrected 2026-08-06: it had read `GREEN (proposed)` since the row was written and was never updated after adjudication. **The confirmation is evidenced, the DATE is not** — a marker is written only on a gate-satisfied terminal state, and the only other such state is a round-cap waiver, which would have left a `WAIVED` line in `.clavity/agy-marks/skipped.log`; there is none for this HEAD. No confirmation date is recorded anywhere, so none is invented here | folds 309fb4e, c72d94b, 8889473; brief `.clavity/seams/capstone-cost-hygiene.md` (rewritten per round). **R1 GREEN rejected as false** — all six seats returned "no new findings" while restating the driver's own do-not-re-raise ledger as their "where looked", and one claim (`all hooks maintain set +e`) measured false. R2 RED with 3 findings, all verified true by counting. R3 GREEN rejected — its Fold Auditor cited `_partition.md:17` and declared it dated when it was not. R4 GREEN, and its one directly-checkable citation quoted the fixed line verbatim |
| 2026-08-06 | `c4cfce4..96d1178` (backlog triage — docs-only) | 3 | **GREEN — owner-confirmed 2026-08-06.** Marker `96d1178`, the REVIEWED tip — **not** the ledger commit `ec45a94`, which the peer never saw. HEAD was re-checked at confirmation and had not moved. No `WAIVED`/`SKIPPED` line exists for this HEAD (`skipped.log` holds only the two 2026-08-04 `d0b4cb1` entries), so this is a clean adjudicated GREEN rather than a gate-waiver | folds `e0afd0c`, `eddcf7c`, `96d1178`; briefs `.clavity/seams/backlog-triage-capstone{,-r2,-r3}.md`. **The owner chose to run a capstone over a range with ZERO executable code** — 12 markdown files — and it was the right call: **the peer refuted a committed claim of mine in BOTH of the first two rounds, and both times it was right and I was wrong.** R1 killed the Task-4 stamp (`AgyView.cs:83` is inside `TryTakeGuidanceBlock()`, gated at `:71` by `Interlocked.Exchange(ref _guidanceDelivered,1)`, and `AskAsync:178-181` sends `var outgoing = message;` after the T4b comment). R2 then killed **my retraction** (`main.rs:641-647` passes `HeaderState::Absent` under *"The PEER gets the ask only"*, and the `read_combined` I cited at `:892` is inside `fn doctor()` at `:859`) — so the item is resolved on BOTH drivers and the stub is now closed as obsolete, not re-scoped. **One cause both times: one driver measured, the other INFERRED from a grep hit whose enclosing function I never opened.** R3 GREEN, four seats, all `no new findings`; I re-verified its two load-bearing claims independently — `TryReadCombined` has exactly one call site and `golden_header::apply` exactly one production call site, whose only caller passes `Absent`. Peer also contributed failure mode 6 (prose-oracle rot), verified live: `McpTools.cs` is in `Clavity.Mcp` while neighbouring stamps scope to `Clavity.Ls/*.cs`. No review-only breach: tree clean, HEAD and reflog tip unchanged, scratch dir empty |
| 2026-08-04 | fe38993..d0b4cb1 (AGY-ANOMALIES capture gap) | 1 | **GREEN — owner-confirmed 2026-08-04**, with one UNVERIFIED finding surfaced and its risk explicitly ACCEPTED (`Agent\|Task` matcher anchoring; audit line in `.clavity/agy-marks/skipped.log`) | brief `.clavity/seams/capstone-anomaly-capture.md`; five seats (Cascade Analyst, Protocol Pedant, Mechanism Gamer, State Corruptor, Boundary Smuggler), all "no new findings", 3/3 spot-checked citations quoted the cited line verbatim. No review-only breach: status clean, HEAD and reflog tip unmoved. **The seven defects that mattered were all found earlier, by RUNNING the suites during execution, not by any panel** — four of them one family: an assertion satisfied by the ABSENCE of its subject (equality between two silent channels; a source scan over a missing file; a `.Count` on a PowerShell-unwrapped scalar; `-Match '2 untriaged'` matching `12`). **Driver's own round found what the panel did not:** the two SessionStart halves were probed with and without `jq` (agree 3/3, silent/silent — control fired, so non-vacuous), and the `Agent\|Task` matcher-anchoring question was raised and could NOT be settled here — logged UNVERIFIED below rather than folded |
| 2026-08-07 | `88a58a1..36c32e3` (plan 3: ROADMAP §12a guard-attribution caveat + §12b `UserPromptSubmit` jq-absent coverage) | 2 | **GREEN — owner-confirmed 2026-08-07.** Marker `.clavity/agy-marks/agy-capstone.head` = `36c32e3`, the REVIEWED tip, deliberately NOT ambient HEAD (`efa0f0c`, this ledger commit, which the peer never saw) — so the gate correctly re-arms on the next trigger. No `WAIVED`/`SKIPPED` line exists in `skipped.log` for either SHA, so this is a clean adjudicated GREEN and not a gate-waiver | Briefs `.clavity/seams/plan3-capstone{,-r2}.md`. Suites at the reviewed tip: **fast 348/0, slow 263/0 with a genuine `Tests completed in 763.71s` line**. All five files in the range are `M` — none added — so the `justfile` registration list needed no change, verified rather than assumed. No review-only breach in either round: HEAD unchanged, empty diff, empty porcelain; R2's only write was `b1-measure.ps1` inside the designated scratch dir. **NO code fold was needed. The capstone's value here was one fabrication caught and one coverage gap surfaced.** R1 returned GREEN across five seats and **its substance held up under measurement**, but **three of its citations were stale by exactly the +19-line offset this change introduced** (`agy-consult-guard.Tests.ps1:115-119`/`:121-127`/`:39-57` are really `:134-138`/`:140-146`/`:41-58`) — i.e. reproduced from the brief and the ROADMAP rather than read from the file. R2 was narrowed to **eight numbered quote-backs plus a calibration control whose answer I already knew**; **all 8 quote-backs were literally correct** and **the control passed exactly** (4 `emit` calls at `:40`/`:49`/`:57`/`:90`; only `:90` instructs a revert; the caveat belongs only there — matching my own enumeration). **But R2 still fabricated a SUPPORTING citation**: it claimed schema correctness is guarded by "sibling tests at `:228` and `:274`", which are really a `New-Item -ItemType Directory` setup line and a backtick byte-check. **The claim that citation was propping up is TRUE and the mitigation is FALSE** — grepped and confirmed: **no test in `agy-consult-guard.Tests.ps1` pins the revert guidance, the targeted-checkout instruction, or the seven axes at all**, so a message gutted down to the three matched substrings would pass. That is a TEST-SAFETY-NET gap, not a defect in shipped behaviour — **routed to AGY-TEST-AUDIT rather than folded here**, because "would the tests catch the next regression?" is precisely the orthogonal question the capstone does not ask. B1 (the escape nobody had checked) was measured by BOTH sides independently and agreed: the two characters `\`+`n` before `${paths}` reach the consumer literally, because bash does not expand `\n` in double quotes and `jq --arg` then escapes the backslash — **pre-existing, cosmetic, below the severity floor, deliberately not fixed** |
| 2026-08-05 | f43f364..a6bcdf2 (SessionStart capture) | 4 | **GREEN — owner-confirmed 2026-08-05.** Marker `.clavity/agy-marks/agy-capstone.head` = `a6bcdf2…`, the REVIEWED tip, deliberately NOT ambient HEAD (`d32e1f4`, a docs-only commit made after round 4); the gate therefore re-arms on the next trigger, which is correct-by-design. **And the thing this ledger cannot record from a review alone: the recorder was then PROVEN TO FIRE** — hot-copied into the installed plugin, session restarted, and a real `{"v":3",…,"source":"resume"}` row landed. That is the measurement v17 never had, and the inference it died on | folds `ea520a4`, `cfd4a25`, `a6bcdf2`; brief `.clavity/seams/capstone-sessionstart.md` (rewritten per round). **The highest-yield capstone in this ledger: 9 real defects across rounds 1-3, every one verified by driver-side measurement before folding, and NONE of them found by the twelve rounds of plan review, my own review, five implementer subagents, or 532 passing tests that preceded it.** R1 (3): a LOCALISED sort key — `ConvertFrom-Json` returns `[DateTime]`, not the ISO string, so `[string]` formatted it per culture (`el-GR` → `08/05/2026 …`) and `-Last N` kept the OLDEST sessions; a merged session losing a transcript a later fire named; and `keep = $g[0]` being unpinnable. R2 (5): **the v15 alarm was SILENT during a live outage** — it summed only completed sessions, and the session you run the report from is by definition still running; plus an unpinned intra-group sort, `began , 2 fires` on an empty source, a dead `first_seen`, a lying test title. R3 (1+3): **a failed `jq` reported as a CLEAN scan** — PowerShell `try/catch` does not catch a native non-zero exit, so a truncated transcript (jq exit 5, measured) had its PARTIAL count stamped `ok`; plus `Get-Num` killing the whole report on `[int]"N/A"`, and two correct-but-invisible branches. R4 CLEAN across three seats, and its four mutation predictions each matched a mutation I had already run independently; its one novel claim (`Sort-Object` does not clobber `$LASTEXITCODE`) I measured separately and it held. **All four R3 guards mutation-proven — four simultaneous mutations produced exactly four matching failures.** One finding REJECTED by measurement (an ASCII sub-second sort inversion: real ordering, unreachable input — the peer concurred, and the overclaiming COMMENT was corrected instead of the code). One AGY-NEGOTIATE, owner-directed, on the `-Last 0` default: converged on keep-the-default-plus-a-notice, **with the peer correctly pressing that my "silent window drift" argument was over-weighted** — an unbounded store already drifts. No review-only breach in any round: status clean, HEAD and reflog tip unmoved (the one dirty file at R1's after-snapshot was my own edit, checked before attributing it) |
| 2026-08-07 | `d895cf3..382396e` (backlog-triage runbook §2 rule inversion; LS-bridge plan) | 2 | **GREEN — owner-confirmed 2026-08-07.** Marker `.clavity/agy-marks/agy-capstone.head` = `382396e`, the REVIEWED tip — deliberately NOT ambient HEAD, which had moved to the ledger-row commit `609d0a8` that is by construction unreviewed. No `WAIVED` line exists in `skipped.log` for this HEAD, so this is a clean adjudicated GREEN rather than a gate-waiver. | folds `382396e`; brief `.clavity/seams/capstone-runbook-and-plan.md`. **R1 PASS-WITH-ADVISORIES, 3 findings, each verified by measurement before folding.** M1 was real and WORSE than stated: the section under review had itself endorsed the `inbox-snapshot` oracle as *sound*, but that entry names THREE alternative mitigations and ranks FIRST a snapshot inside the `curate-commit` binary, which leaves no trace in `hooks.json` — so the oracle cannot see its own preferred fix. A1 was a real defect with a WRONG diagnosis: the peer called §3/§4 stale older wording; measured, they say *NOT evidence of FIXED* while new §2 said *not evidence of OPEN* — opposite guards, both needed, neither pointing at the other. Folded as the mirror-pair, not as a duplicate. B1 ordering, folded. 🔴 **R2 returned CLEAN across 3 seats and I did NOT bank it.** Its Literal Implementer walkthrough cited `GetFilteredTrajectoryAsync` as the `agy_look` implementation — **that symbol does not exist** (`LookAsync`, `AgyView.cs:103`), and its §13 line citation was off. Conclusion right, evidence partly fabricated — the documented direction-right/specifics-wrong pattern, inside a clean verdict. **Verified independently instead:** I re-ran the remnant sweep myself (no occurrence of the inverted rule survives anywhere in the file) and did the walkthrough myself (`agy-look-tail-truncation` names TWO mitigations and its stamp checks BOTH, so §2 yields an unambiguous `open`). |
| 2026-08-07 | `3a90fff..345f268` (LS-bridge truthful diagnostics — plan 1 of Phase 3) | 5 | **GREEN — owner-confirmed 2026-08-07.** Marker `.clavity/agy-marks/agy-capstone.head` = `345f268`, the REVIEWED tip — deliberately NOT ambient HEAD, which moves to this ledger-row commit. No `WAIVED` line in `skipped.log` for this HEAD, so this is a clean adjudicated GREEN. | folds `2b904a3`, `bf66b61`, `345f268`; briefs `.clavity/seams/ls-bridge-capstone.md` (rewritten per round). **8 findings: 6 folded, 1 refuted by measurement, 3 pre-existing costs already disclosed in the plan.** 🔴 **The two best findings were defects in MY work, not the plan's.** (a) `BoundedView.Summarize` had NO step-count cap: a tool step has null text, so `cost` is 0 and `used + cost > budgetChars` can NEVER fire — MEASURED **750 steps emitted under a 900-char budget**. The codebase already knew this failure mode (`MaxActivitySteps` carries a comment saying exactly why) and had applied the cap only to `ProjectAskReply`/`agy_ask`, leaving `agy_look` exposed. (b) **My own round-1 fold reintroduced the status-vs-hint contradiction the change existed to remove** — widening the hint to name quota left `StatusFor` still asserting `payload_too_large`. Escalated to the owner as a wire-contract fork per the plan's own rule; ruled A1 (`resource_exhausted`, status mirrors the gRPC code) + the `auth_failed`/`invalid_request` split. **Detail-string discrimination was considered and REJECTED: it couples a wire status to `Grpc.Net.Client`'s internal message text, which is not a contract.** The peer's counter-argument changed my position on both forks, so the owner received one converged recommendation rather than a split. **AB-2 REFUTED by measurement** (one production call site; `McpTools.cs:16` never passes a budget) and the peer accepted the refutation. ⚠️ **3 of 8 round-1 file:line citations pointed PAST END OF FILE** (`ChannelDown.cs:216` in a 73-line file; `LsChannel.cs:240` in 60; `LsChannelIntegrationTests.cs:324` in 106) — findings still judged on merit, and one was the round's best. Citation accuracy improved to EXACT by round 5. 🔴 **Verifying round 4's clean verdict surfaced a gap the peer did NOT raise:** `StatusFor`'s exhaustiveness was guarded only by a CODE COMMENT — the same thing that failed to prevent finding (b) — so `345f268` converts it into an enforced test, control-proven by removing the `auth_failed` arm (`faults sharing one status: channel_down <- TransportDown + AuthFailed`). **Round 5 exists solely because `345f268` landed after round 4's GREEN; it folded NOTHING and nothing was committed after it, which is what makes this GREEN legitimate.** Suites 155→164 (Ls) and 53→55 (Integration), build 0/0. |
| 2026-08-08 | `43c1a9c..614ca00` (AGY-SCOPE disposition amendment — ROADMAP §7) | 4 | **GREEN — owner-confirmed 2026-08-08.** Marker `.clavity/agy-marks/agy-capstone.head` = `614ca00`, the REVIEWED tip — deliberately NOT ambient HEAD, which moves to this ledger-row commit. No `WAIVED` line in `skipped.log` for this HEAD, so this is a clean adjudicated GREEN. | folds `4fba6d6`, `02acaf7`, `614ca00`; briefs `.clavity/seams/agy-scope-capstone{,-r2,-r3,-r4}.md` (one file per round, not rewritten). **Dispositions, complete under the taxonomy this very range ships: FOLDED ×3 · REJECTED ×1 · DISCARDED-BELOW-FLOOR ×1 · no UNVERIFIED, no DEFERRED.** R1+R2 clean on `43c1a9c..847cb4e` (6 seats then 4 fresh seats); **R3 found the run's one real defect** — `ROADMAP.md:658-660` still asserted §7 was *"still undesigned and goes to the Phase-2 owner gate"*, a FORWARD-LOOKING claim whose decision had since resolved; R4 reviewed the folds and was clean. 🔴 **THE PLAN WAS OVERRIDDEN, deliberately:** it said to exclude the ROADMAP commit from the capstone, but `4fba6d6` FOLDS three defects, and `agy-capstone/SKILL.md:78-81` requires the final clean round to cover the fold commits — excluding it would also make the marker unable to equal HEAD, since the auto-fire hook reads `content == current HEAD`. **The skill won over the plan.** 🔴 **REJECTED by measurement:** "the four `SKILL.md` mirrors are enforced only by convention" — `scripts/check-seed-artifacts-synced.sh` is DISCOVERY-based and its `divergent()` deny-list names none of them, so drift fails closed. 🔴 **DISCARDED-BELOW-FLOOR (anti-sweep, named here):** `ROADMAP.md:552` — guard cited: it is a dated *"Update 2026-08-01"* point-in-time record, superseded by the SHIPPED blockquote at `:498-510`; distinguished from the R3 defect precisely because it is past-tense, not forward-looking. 🔴 **The epic's signature defect re-fired INSIDE MY OWN FOLD, one turn later:** `02acaf7` closed with *"…is an owner call, not a triage one"*, which the owner's ruling made stale minutes after it was written (fixed at `614ca00`). **A sentence that defers to a pending decision goes stale the moment that decision lands.** 🔴 **The peer beat its own record for the first time in this epic:** all 14 numbered anchor quote-backs across 4 rounds were literally correct — *including end-of-file line numbers, previously actual+1 on every file every time*. What changed, and is worth reusing: terminal verdict demanded on LINE 1, an explicit severity floor naming what does not count, an explicit licence that *"no new findings"* is a CORRECT answer, a do-not-re-raise ledger grown to 17 numbered items and inlined every round, and — the highest-yield move — **demanding a CENSUS TABLE (one row per item inspected, with `file:line`) instead of a conclusion**: R2's unfalsifiable *"all enumerations across the repo have been updated"* became 17 cited rows only once the table was demanded, and the seat framed that way found the real defect the next round. Suites green pre-capstone: `test-scripts-fast` 364/0 (512s), `test-scripts-slow` 263/0 (766s); the three subsequent commits are docs-only and no test asserts on them (measured). Task 6's mutation rows were control-proven non-vacuous: `Failed: 10` with the rows present but the checker unchanged, `Failed: 0` after. |
| 2026-08-16 | `eeaa24c..bc4813b` (anomaly hot-fix batch — the PLAN artifact, `docs/superpowers/plans/2026-08-14-anomaly-hotfix-batch.md`) | 50 numbered rounds; **41 fold commits since the panel GREEN** | **GREEN — owner-adjudicated 2026-08-16.** Marker `.clavity/agy-marks/agy-capstone.head` = `bc4813b`, the REVIEWED tip; HEAD was re-checked at adjudication and had **not** moved. No `WAIVED`/`SKIPPED` line exists in `skipped.log` for this HEAD. ⚠️ **TWO CAVEATS, recorded because this row must not read cleaner than the run was.** (1) **The final round FOLDED three defects and no clean round followed it**, so `SKILL.md:78-81` ("the final clean round MUST cover those fix commits before you may declare GREEN") is NOT satisfied. Every finding across the run has a disposition — none is live — but r50's folds are unreviewed by construction. The owner adjudicated GREEN one round early, knowingly and on the record. (2) **It was not a waiver either:** no findings remained open, so `round-cap` does not describe it and no audit line was written. | 🔴 **THE RUN'S REAL FINDING IS THE RUN ITSELF: this capstone was mis-targeted for 41 commits.** `agy-capstone/SKILL.md:12` says the discipline reviews *"the committed implementation (the executable code and its tests), **never the plan artifact**"* — and `eeaa24c` is titled *"fold panel round 17 — **panel reaches GREEN**"*, i.e. the plan's correct review (the adversarial panel, AGY-AFTER) had ALREADY completed. Everything after it applied a code-review discipline to a document. **That is why it could not converge:** a capstone stops on "no reachable behavioural defect in code that RUNS", and a plan does not run, so the stop condition never fires; its severity floor (*nothing touching correctness/safety/contract/completeness*) is meaningless on prose, where everything is arguably completeness. 🔴 **And the medium has no referential integrity, which makes the defect supply unbounded:** at `aec3639`, the commit that INTRODUCED the FIXTURE HYGIENE census, the factories sat at lines 429/1352/1607/3772/4753 while the committed text cited 422/1345/1600/3765/4746 — **every one off by exactly 7**, i.e. measured, then invalidated by an insertion above them IN THE SAME COMMIT. Born wrong, not drifted. **The identical defect then re-fired in my own r48 fold**, where adding Task 13 Step 7b pushed the `yq` queries from `:4969` to `:5043` in the commit that cited `:4969`. 🔴 **SEVERITY DECAYED MONOTONICALLY AND THAT IS THE STOP EVIDENCE:** r48's three folds would each change what an executor experiences (an undeclared `yq`, a checklist row missing its `(skip if BLOCKED)` marker, three steps hardcoding `/tmp` against the plan's own measured finding that `/tmp` resolving is a Git Bash coincidence). **r49's and r50's six would not change what the plan produces at all** — a mutation table claiming one mutation reddens two rows that encode OPPOSITE failure modes, a stale "add this row" for a row that already existed, a control saying "at least one row" instead of naming four, six self-citations of which NOT ONE resolved (one pointed at a blank line) under the words *"VERIFIED across the whole plan"*, and a section contradicting itself about which fixture factories exist. **A verification claim whose every pointer is wrong reads as MORE trustworthy than one with no pointers — that is what makes this class worth recording even though it changes no output.** Folds this stretch: `d2ca568` (r48), `d9d352b` (r49), `bc4813b` (r50); briefs `.clavity/seams/2026-08-15-capstone-14h-fold.md` (rounds 24-34), `2026-08-15-capstone-r47-resumability.md`, `2026-08-15-capstone-r48-prerequisites.md`, `2026-08-16-capstone-r49-mutation-controls.md`. ⚠️ **r50 was DRIVER-ONLY — no peer consult ran**; the axis was mechanical and every finding was measured, but only half the discipline executed, and that is stated rather than glossed. **All three r49 folds were the driver's; the peer's continuation produced zero and FABRICATED a row name** (`a raw CR (0x0D) must never be baked into the Rust literal` appears nowhere; the real row is `NORMALISES CRLF to LF`). 🔴 **A transport defect worth carrying forward:** `agy_ask` returns the peer's FINAL message only, so an eleven-item review arrived as the single sentence *"I'm awaiting your next round or instructions"* — recovered via `agy_look` on the trajectory, and every later payload states that the findings must be the last message. **The exit was itself decided by a seated AGY-FIRST consult + one AGY-NEGOTIATE round, converged:** the peer's Blindspot Auditor proposed a per-task capstone, then withdrew it once measurement showed all 15 executing tasks already end in their own commit after their own tests — *"E was C carrying a false premise"*. Oracles at the reviewed tip, both controls behaving: `sh -n` over **76** shell blocks 0 failures, `Parser::ParseFile` over **21** PowerShell blocks 0 failures. ▶ **The REAL capstone for this work has not run yet** — it is Task 16 Step 5, over the committed implementation, after the plan executes. |
| 2026-08-17 | `69b1c86..1022f8f` (post-audit range: two AGY-TEST-AUDIT rounds, an anomaly triage, the curate drain, and the cheatsheet promotion) | 3 | **GREEN - owner-confirmed 2026-08-17.** Marker `.clavity/agy-marks/agy-capstone.head` = `1022f8f`, the REVIEWED tip; HEAD was re-checked at confirmation and had NOT moved, so no unreviewed commit was greened. No `WAIVED`/`SKIPPED` line exists for this SHA, so this is a clean adjudicated GREEN and not a gate-waiver | Briefs `.clavity/seams/capstone-post-audit.md` (rewritten per round). Folds `598fc02`, `6b86445`, `1022f8f`. Suites at the reviewed tip: agy-shield-lib **39/39**, generate-cheatsheet-literals **18/18**. No review-only breach in any round: tree clean, HEAD and reflog tip unmoved throughout. **THE RANGE WAS DECLARED DONE TWICE BEFORE THIS CAPSTONE AND WAS NOT.** R1 found a **vacuous ordering assertion I had written that same day and described in its own commit message as 'the load-bearing one'** - the three C# rows asserted the rust target was byte-unchanged after a C#-side refusal, but the fixture copied the CANONICAL `.rs`, which already holds exactly the bytes a regeneration produces, so an errant early write rewrote identical content invisibly. Proven end-to-end: with the generator patched to write rust BEFORE validating C#, all three rows still passed. **My own six round-B mutants missed it because every one broke a GUARD and none moved a WRITE** - a mutant set that perturbs one construct certifies only that construct. R1 also found the marker-hygiene `AfterAll` deleting a CONCURRENT run's live markers (it filtered on a 32-hex key SHAPE that a second run of the same suite reproduces); confirmed by injecting two markers mid-run - a 32-hex victim (deleted) and a dashed-GUID bystander (survived), which BOUNDS the defect rather than merely detecting it. R2 then found a defect **inside R1's fix** (the generator's two writes were sequential with no rollback, so a throw between them left the tree half-applied - measured with a read-only C# target: exit 1, rust rewritten, C# stale) and a coverage gap in R1's other fix. R3 seated a **Blindspot Auditor at MY DISPOSITIONS rather than the code** - six seats had reviewed the code, none had reviewed my reasoning about it - and all three judgements held, with the peer reconstructing the mechanism independently rather than agreeing. 🔴 **THE PEER'S FINDING WAS RIGHT AND ITS REMEDY WRONG IN ALL THREE ROUNDS**: R1 proposed a junk rust target (would fail rust validation first, so the rows never reach the C# guards they exist to test); R2 proposed a leak consequence measurement refuted; R3 proposed a C# assertion that MEASURES AS VACUOUS (9067 bytes before and after - a read-only file fails at OPEN and is never truncated), which would have installed the exact defect R1 caught, inside the fix for a finding about that same row. Folded as a stated scope limit instead of faked coverage. ⚠ **SIX OF MY OWN CHECKS RAN GREEN AND MEANT NOTHING** across this range, three of them inside one finding: a narrow `-FullNameFilter` producing a false whole-suite coverage claim; **the mutant's own leaked markers contaminating the re-measurement** (a truncated key mints a FIXED name carrying no run tag, so `AfterAll` cannot clean it, and the stale marker debounced the next run's report - flipping the verdict between two runs of the IDENTICAL mutation); and a filter that never executed the row being verified. **The durable rule: restoring the source byte-identically is NOT restoring the environment - a mutant's side effects outlive the source restore and poison the next measurement.** |
| 2026-08-10 | `c17bcbe..06a39af` (injected-context gate - **STAGE 1** of the two-stage branch plan, `docs/superpowers/plans/2026-08-08-injected-context-gate.md`) | 20 | **GREEN at `06a39af`.** **ROW WRITTEN RETROACTIVELY 2026-08-18** - see the note below; the contemporaneous adjudication is NOT separately recorded, and this row does not claim one. | folds `78a5bed` (r2), `9c71718` (r17), `76e1ba8` (r17 residue), `54f1b56` + `2fa88e0` (r18), `9f34dc7` (r20). The GREEN sha is independently corroborated: the Stage 2 plan's Goal line pins the full `06a39afe47ae479bcba5101e7142e63f188f9031` as the capstone GREEN its own batch must not invalidate. |
| 2026-08-12 | `cec62ff..49260dd` (**STAGE 2** - the fix batch for the three-product anomaly sweep, `docs/superpowers/plans/2026-08-11-stage2-fix-batch.md`) | **29 fold-rounds across THREE separate capstones**, not one: curate/drain 15 (R1-R14 folded + a clean R15) - hooks/A2 4 - ci+test-registration 10 | **GREEN - owner-ruled 2026-08-18.** This is the ruling that also closed the Stage 1 plan's merge gate (`2026-08-08-injected-context-gate.md:26`, *"the branch does not merge until it is green"*), which had stood unclosed since 2026-08-10. **ROW WRITTEN RETROACTIVELY** - see the note below. | curate/drain folds `00e3291`..`a3ce038` (R1-R14), round count stated by `6c72b5a` - hooks folds `f7236f8`, `6c1c3b3`, `ea6f9df`, `1dc0f71` - ci/test folds `8160bfc`, `6a1e0ec`, `152087f`, `70701a4`, `1ae166a`, `0d796c1`, `301ee5f`, `2938a58`, `82e0f6e`, `49260dd`. AGY-TEST-AUDIT also ran: `ae18bdb` logs its two owner-deferred gaps to `docs/coverage-debt.md`. |
| 2026-08-19 | `77aa257^..08254ab` (actions bumps + agy_ask progress reporter) | 3 | GREEN | folds `b0540a0`, `45efd4d`, `08254ab`; 10 mutants all caught, source restored byte-identical each time; suites 164 + 61. **Notable: the executable source ended IDENTICAL to the pre-capstone commit** (`AgyView.cs` is +18/-1 vs `f8830d0`, all comment) - R1's finding was one I had already measured as inconsequential, folding it introduced a defect R2 caught, and R2's fix introduced a defect R3 caught. What genuinely improved was the SUITE: 4 new tests, 4 vacuous assertions removed, and the delete-the-report mutant now turns 5 tests red where it turned 2. |
| 2026-08-19 | `14a0e41..1748754` (AGY-TEST-AUDIT gap closure + the idle-wait limit-label fixes) | 4 | GREEN (owner-confirmed) | folds `bcd4125`, `0fb47f6`, `1748754`; suites 164 + 64. **R1** found the gap-4 assertions made two tests CI-flaky (`lastProbe` null at `AgyView.cs:234` while the budget check at `:240-244` runs before any probe); its proposed fix - widen the budget to 2s - was REJECTED by measurement, since that un-clamps window 2 and turns the budget-clamped test red. **R2** found the limit label raced timer jitter; instrumenting the decision point over 10 runs / 50 decisions gave a worst-case margin of **+5.1ms** against ~15ms timer resolution, making it a PRODUCTION mislabel, not just a flake. Fixed in `0fb47f6`. **R3 RETURNED A CLEAN ROUND AND MISSED A REGRESSION `0fb47f6` INTRODUCED** - a clamped window ended by the SERVER's timeout with the budget already spent reported `stall`. Found by hand-tracing the case matrix, not by review; fixed in `1748754` with a control that was RED first (`Expected "absolute_max" / Actual "stall"`) and carries a precondition assert so it cannot pass for the wrong reason. **R4** produced the full 8-row case matrix, every row verified against the code. 9/10 mutants caught by their specific intended test; **M9 survives deliberately** (accepted boundary J in `docs/coverage-debt.md`). ⚠ **R4's `[VERIFIED:]` block cited `clavity-dotnet/docs/coverage-debt.md`, which DOES NOT EXIST** - the protocol's one machine-checked anti-hollow-compliance device, partly fabricated; its boundary-J endorsement was re-verified by hand. ⚠ Two earlier probes in this run were themselves broken and produced false verdicts: a mutant parser matching `Failed ` instead of xunit's `[FAIL]` reported all five mutants VACUOUS, and an edge probe measured the right answer by the WRONG PATH (the clamped window beat the server, so the branch under test never ran). |
| 2026-08-20 | `103aa87..20f38cc` (step 2 / ROADMAP `13b` - reply-survival, built then TRIMMED) | **11** | **GREEN - owner-confirmed 2026-08-20.** Marker `.clavity/agy-marks/agy-capstone.head` = `20f38cc`. 🔴 **DISCLOSED: `20f38cc` is R11's OWN FOLD and was not itself sent to a 12th round** - the last round covered `fc5766e..2c4f933`. The owner adjudicated GREEN over the whole work with the fold described in the report, so this marker records an OWNER ADJUDICATION, not a peer-reviewed tip. No `WAIVED`/`SKIPPED` line exists for this SHA. | Briefs `.clavity/seams/step-2-13b-capstone.md` (REWRITTEN every round). Folds `99322e8` `3ac9f2c` `3085c78` `4bb560c` `a8579d0` `0b1a450` `136789a` `fc5766e` `2c4f933` `20f38cc`, plus `3804f41` (driver's own cross-product finding) and the trim `a48fd30`. Suites at the tip: **200 + 80**, discipline Pester 42/42, all gates 0. Envelope held every round (tree and HEAD unmoved; peer wrote only to `.clavity/scratch/`). 🔴 **THE AXIS, NOT THE EFFORT, DROVE THE FINDING RATE.** Rounds 1-3 asked "what is wrong inside these files?" and yielded 4, 4, 2. That lens dried; changing the axis revived it every time - R4 externalities (a CULTURE defect: `$"{utcNow:yyyyMMdd}"` renders 2569 under th-TH, 1448 ar-SA, 1405 fa-IR, so the pruner stopped recognising its own files), R5 the TEST SUITE (**every `SizeAnomaly` assertion was `Assert.False` and every `SIZE WARNING` one `DoesNotContain` - the whole heuristic could have been dead and the suite stayed green**), R6 what it leaves behind, R7 cost + efficacy, R8 argue-against-your-own-findings. 🔴 **SIX OF THE LAST TEN DEFECTS WERE CREATED BY AN EARLIER ROUND'S FOLD**, and R8's own verdict was "the review is generating its own work" - so R8 folded NOTHING by design (all its findings below floor) and the loop stopped. **A review loop can converge on its OWN OUTPUT: stop when the findings are about the FIXES, not only when they dry up.** 🔴 **FOUR TIMES A FIX NEEDED ITS OWN FIX** - R2 caught R1's, R3 caught R1's and R2's, R6 caught R3's, R11 caught R10's on the very next round. **EIGHT PEER CLAIMS REFUTED BY MEASUREMENT**, incl. its R4 headline that Claude's MCP client strict-rejects an undeclared argument and would hard-block the agent (**measured: an undeclared `discipline` passed to `agy_status`, whose schema has NO properties, was ACCEPTED**), and the 8.3 `.mdx` glob claim (refuted with a control that could only discriminate after I renamed its fixture - my first one sorted NEWEST and could never have been deleted). **R10 CORRECTED ME**: I argued a TOCTOU swap degraded safely because every route returns null - wrong, it HANGS, and a hang never reaches a catch. **THE TRIM (`a48fd30`, -1426 lines) IS THE OUTCOME**: the peer recommended revert after rejecting the frame six times; the owner ruled trim. `ReplyArchive` + `ReplySizeHistory` deleted (they caused ~10 of the 19 folds for an advisory warning), `expectEcho` -> `artifactPath` so the DRIVER derives the echo, `[13b] UNCHECKED` latched once per session, CI now runs the integration suite (**it held the only `dotnet test` line in 21 workflows, so every 13b guard had no oracle**), skills cut +82L/6.1KB -> +49L/3.4KB. Efficacy stated in `TerminalToken`'s summary: **case (a) wire-death CAUGHT, case (b) lone-verdict-line NOT** - deleted deliberately, with the measurement in hand. Two masking pairs recorded as accepted boundary D in `docs/coverage-debt.md`. |
| 2026-08-24 | `bd3aa94..f29cd42` (ROADMAP §14g - the agy-observations inbox moved to `~/.clavity/`, plus the Pester v5→v6 CI repin) | 4 | **GREEN — owner-confirmed 2026-08-24.** Marker `.clavity/agy-marks/agy-capstone.head` = `f29cd42`, the REVIEWED sha (HEAD re-checked at adjudication and unmoved). | folds `4079c2c` (claim-by-rename + rollback), `2e8cf63` (R2 accepted boundary), `7e4a10e` (kill-switch split), `343499f` (residue duplication + 4 stale statements), `f655727` (kill-switch fold completed + new suite); brief `.clavity/seams/capstone-14g-pester.md`, rewritten per round. **R2's only finding was REJECTED by measurement** against Inno's own docs. **R3 returned CLEAN and probing its PROSE found a real defect.** **R4 was run past the cap on OWNER RULING, on BOTH a fresh-subagent arm and the peer arm**: the subagent arm found the `f655727` defect, the peer arm was clean on all 6 seats with every citation verified against disk. |
| 2026-08-27 | `f29cd42..fac6fa5` (27 rounds; the folds this round produced run `f9f2998..e60ad19`) | **27** | **GREEN - owner-confirmed 2026-08-27, under the NEW stopping rule adopted the same day.** Marker `.clavity/agy-marks/agy-capstone.head` = `fac6fa5`, the sha round 27 actually REVIEWED - deliberately NOT ambient HEAD. The five fold commits `f9f2998..e60ad19` are this round's own output and are UNREVIEWED by construction, so the gate correctly RE-ARMS on them. That is the discipline working, not a bug. | **THE ROUND THAT CHANGED THE RULE.** Both arms ran as READERS on disjoint seats, blind to each other. Seat G (peer-free subagent) audited the 152 `MEASURED` claims this range added - every one written by the driver, none checked by anything - and returned 9 findings with 21 rows CLEARED. Seat H (live peer) audited CALLERS: for every gate, recipe, hook and suite, not "does it pass" but "what executes it". 13 findings total, all dispositioned. **Folds: `f9f2998` `9fc63b8` `2ec9b85` `a163d5a` `e60ad19`.** Brief `.clavity/seams/capstone-gapclosure-r27.md`; replies `.clavity/scratch/capstone-r27/`. **THE FINDING OF THE SERIES: `check-injected-context` had been RED AT HEAD for 29 commits and 15 capstone folds** - two U+1F534 entered a SHIPPED skill at `d5d0c51` (round 8's OWN fold) and `c23bf64` (round 18's). No round caught it because rounds READ code and a gate only speaks when EXECUTED; CI never caught it because all 57 commits were UNPUSHED. It was one push from arriving red. **Also: five suites guarded `agy-autotrain/hooks/**` and NO workflow triggered on it, under a comment promising a checker whose own source records that half as DELETED. And `99449a3` converted the `.sha256` write in four build workflows and left the FLAGSHIP on `Set-Content`, shipping the one companion `sha256sum -c` rejects (measured: `0d 0a` vs `0a`).** **One fact was in FOUR places, not the three the reviewer found - the fourth was in a TEST**, exactly where this repo's law says a copied rationale hides. **WHY THIS ROUND ENDED THE RUN.** Round 27 was adjudicated under a stopping rule agreed with the live peer over three negotiation rounds that day and written into `agy-capstone/SKILL.md` (both copies) as *The stopping rule*: a `defect` is now DERIVED as BLOCKING or DEBT, and only BLOCKING blocks GREEN. Of round 27's 13 findings, **4 BLOCKING** (the red gate and the CRLF companion by Rule 0 - a command exits non-zero; the unreachable tree by Rule 1; the false guarantee by Rule 4) and **8 DEBT** (stale counts, drifted rationale, a wrong citation, duplicated facts). All four BLOCKING items were fixed and committed before adjudication, so the round closes. ⚠ **The peer's first mechanical draft classified the RED GATE as DEBT** - internal markdown, no guard asserted - which is why Rule 0 exists and is evaluated FIRST: a lexical rule asks what a line RESEMBLES, only a command MEASURES. |
| 2026-08-28 | `fac6fa5..209f929` (ROADMAP §18 - the knowledge STORE, its gate, and the driver-cheatsheet EXTEND path; the folds this round produced run `f4a93f9..27e6d16`) | **7** | **GREEN - owner-confirmed 2026-08-28.** Marker `.clavity/agy-marks/agy-capstone.head` = `209f929`, the REVIEWED tip; the r7 fold `27e6d16` is this round's OWN output and correctly RE-ARMS the gate. | Briefs `.clavity/seams/capstone-18-store{,-r2..-r7}.md`. Folds `f4a93f9`, `c49ccba`, `9e41de4`, `9cf1920`, `69b2d41`, `bb6b9a4`, `88198b1`, `bbc32ac`, `209f929`, `27e6d16`. **R5 ended in a REDESIGN, not a patch:** asked outright whether the dangling-consumer gate was the wrong design, the peer said delete it - four versions had each INFERRED production from prose and each inference was measured wrong - and 211 lines of heuristics became a 156-line exact-match on an explicit producer marker. **A MUTATION AUDIT (`bbc32ac`) then proved all 9 guards non-vacuous** and found two holes 5 rounds of review had not: a test passing for the wrong reason, and a guard with NO test. **R7 used a fresh axis - audit the CLAIMS, not the logic** - and the peer checked 26 of 26 `MEASURED` sentences for 1 finding. ⚠ **R7's reply arrived TRUNCATED** (no echo line, no terminal token); recovered via `agy_look` per the 13b rule, so the DEBT classification and verdict are the DRIVER's derivation, not the peer's - recorded here rather than laundered. |
| 2026-08-28 | `209f929..27d3c90` (ROADMAP 18 DELTA - the re-capstone forced by AGY-TEST-AUDIT, which found a REACHABLE defect in code the 7-round capstone above had passed) | **2** | **GREEN - owner-confirmed 2026-08-28.** Marker `.clavity/agy-marks/agy-capstone.head` = `27d3c90`, the REVIEWED tip; the r2 fold `337f199` is this round's OWN output and correctly RE-ARMS the gate. No `WAIVED` line in `skipped.log` carries a HEAD inside this range, so this is a clean adjudicated GREEN. No review-only breach in either round - tree `?? TODO.md` before and after, HEAD unmoved. | Briefs `.clavity/seams/capstone-18-delta{,-r2}.md`. Folds `05e7188`, `27d3c90`, `337f199`. **WHY THIS ROUND EXISTED: the audit found the defect the capstone missed, in a filter the PEER had flagged as untested and then DISCARDED below the severity floor** - reasoning that a broken exclusion would only "search more files and run slower", which considers the over-inclusive direction alone. `$_.FullName` is ABSOLUTE, so a clone under any `bin/target/obj/node_modules` segment excluded EVERY file and reported every consumer dangling. Measured with a paired control: identical trees, exit 0 clean vs exit 1 under `bin`. **A recurrence of R4's absolute-path finding in the SAME file.** 🔴 **R1 returned 3 BLOCKING and all three resolved differently, which is the transferable part: F1 REAL but its stated trigger (non-FileSystem provider) did NOT reproduce - and probing the same mechanism found a WORSE sibling it never named, a PSDrive root where the subtraction does not throw but silently yields mangled paths. F2 a FAIR finding with a REGRESSIVE fix - `^`-anchoring the exclusions was measured to take the scan 3.940 -> 40.006 files, re-admitting 36.066 build artifacts; the prose half was accepted, the fix rejected. F3 REFUTED by a failing control - the assertion it called vacuous FAILS on a file that really begins EF BB BF.** **R2 conceded both probe questions with correct reasoning** (and supplied the mechanism for F3: Pester v4 unwrapped piped collections, v5 collects them first - recorded in the test ATTRIBUTED and marked unverified), found nothing new, and its one discarded item was verified sound. **Closer exact in BOTH rounds.** |
| 2026-08-30 | `87dc81f..62eb46f` (step 5 — roadmap 17a per-repository shield markers + 19 exit-code collapse) | **11** | **GREEN — owner-adjudicated 2026-08-30.** Marker `.clavity/agy-marks/agy-capstone.head` = `62eb46f`. ⚠ **DISCLOSE: one `BREACH-REVERTED` line exists in `skipped.log` for this range** (round-10 counter-turn; peer script redirected output to repo-root `out.txt`/`out2.txt` instead of its scratch dir — caught on the after-snapshot, both untracked and containing only shell trace, removed, no tracked file touched). No `WAIVED` or `SKIPPED-UNREACHABLE` line whose HEAD falls inside this range. | Briefs `.clavity/seams/capstone-step5-r1.md` … `-r11.md` (a fresh brief per round). Folds `37e2f0f` `3a499e9` `1b8b1cb` `9f0c52b` `79b7cd0` `0282c9e` `d9548e1` `1b09e76` `1c81a47` `62eb46f`. Suites at the reviewed tip: agy-shield-lib **44/0**, agy-mark **30/0**, test-suite-registration **8/0**; gates `check-injected-context`, `check-knowledge-store`, `check-dangling-consumers`, `check-core-integrity`, `check-plugin-namespace` all exit 0; both plugin pairs re-verified byte-identical every round. 🔴 **EVERY ROUND 1-10 WAS RED, and the axis is what kept it that way — the yield never decayed while the lens kept moving.** 🔴 **THE HIGHEST-VALUE MOVE OF THE WHOLE CAPSTONE WAS PROBING A GREEN.** Round 5's panel returned GREEN on five bespoke seats; a numbered quote-or-DOES-NOT-EXIST probe with an out-of-range control, plus ONE good-faith question — *"name a file you listed in [VERIFIED:] but did not open"* — got an honest admission that it had read ONE of five in full and covered the rest by diff. **The file it had not opened was `agy-mark.sh`, where roadmap 19 lives.** Re-asked to read it whole → RED with three real defects. **A `[VERIFIED:]` block proves intent, not coverage.** 🔴 **THE PEER DEFEATED THREE GUARDS I WROTE, on three separate rounds** — the sweep-ordering row failing open to a hoisted gate (r2), the log row certifying one field of four (r4), and both r6 rows pinning the *shape of a violation* rather than the rule (r7). **A guard you attacked once is not a guard you attacked well.** 🔴 **ITS SINGLE BEST FINDING (r8): round 6's redirect-order fix had been applied to `agy-shield-lib.sh` and not to `agy-mark.sh`** — measured, a raw OS diagnostic printed ABOVE the script's own message. **That is Law 3 and I broke it: I swept the file I was editing instead of the repo.** Sweeping properly found the pattern in six further hook files, out of range, captured as tracked anomalies rather than edited. 🔴 **TWO PEER FINDINGS WITHDRAWN ON THE PEER'S OWN MEASUREMENT (r10)** — I sent it to measure rather than pasting my results, per the counter-turn rule, and it independently reproduced mine: the sweep latch is bounded exactly as the owner ruling states, and `git add -A` stages nothing under `.clavity/`. **Sending it to the source rather than to my conclusion is what made the retraction possible.** ⚠ **MY OWN ERRORS, RECORDED:** round 10's brief OMITTED the do-not-re-raise ledger, which is why item 9 was re-raised in good faith; a new test row shipped STRUCTURALLY VACUOUS until a mutant caught it (**`Start-Process -ArgumentList` cannot deliver an embedded newline — it splits the argument**); one assertion was over-broad and failed on correct behaviour; and a whole mutation run reported "both mutants applied" when neither had, caught only by `cmp` against the backup. **Accepted boundaries added: N** (`head`'s non-atomic marker write — an absent marker re-fires the discipline, which is the design's own safe direction, and the obvious `mv`-based fix moves the temp INSIDE a directory target, which a named row forbids). **Entry M was RETRACTED** — it claimed the sweep ordering was unpinnable; it is pinnable by intercepting `find` with a shell function and reading the shield's CONTENT at that instant, mutation-proven against three mutants. |
| 2026-08-31 | `6c998ce..274afbd` (the review-only consult envelope: an 8th ignored-paths axis on `agy_guard_quad`, plus the two skill-envelope gaps) | 8 | **GREEN - owner-confirmed 2026-08-31.** Marker `.clavity/agy-marks/agy-capstone.head` = `274afbd`. **A CONTRACT CONFLICT IS RECORDED RATHER THAN PAPERED OVER:** `agy-capstone/SKILL.md:403` says write ambient `HEAD`, while this ledger's 2026-08-08 row says the REVIEWED tip, "deliberately NOT ambient HEAD, which is the ledger-row commit and is unreviewed by construction". The skill's contract was followed and the marker written AFTER the ledger commit, so it debounces as intended; the older row's objection stands and is unresolved. **TWO REVIEW-ONLY BREACHES, both reverted and logged to `skipped.log`:** R1 created `./test.sh` (77B `set -e` probe); R8 created `./%s` (0B, botched printf) and `./test-lib.sh` (27KB copy of the guard lib). All pure creates, nothing overwritten, all preserved under `.clavity/scratch/capstone-envelope-r{1,8}/`. Both happened despite an enumerated prohibition AND a sanctioned scratch directory the peer used for everything else. **`git status` caught R1's only because it was not gitignored - one directory over, under `.clavity/`, the gap this very range closes would have hidden it.** | **21 verified defects folded across 8 rounds**, fold commits `a2bd0b3`, `67ca7ef`, `93e2d05`, `4fc8f62`, `7cc159c`, `0e58094`, `d16c0b7`, `a5a9d74`, `f0755c8`, `21da725`, `e067000`, `02dd937`, `41ae0e3`, `274afbd`; briefs `.clavity/seams/capstone-envelope-r{1..8}.md` plus the shape consult `.clavity/seams/seams-monitoring-approach.md`. Final suite **39/39**, payload gate 3/3, five repo gates RC=0, both hooks fail open (`pre`/`post` exit 0 on `{}`), pairs byte-identical, shipped hooks pure ASCII. **THE FIX-SPAWNS-A-DEFECT PRIOR HELD SEVEN CONSECUTIVE ROUNDS** and broke only at R8, which found zero code defects. **The dominant defect class was CONSTANT ANSWERS - a guard returning the same value regardless of input while reporting clean - and it recurred THREE times** (`NOHASH`; the census all-`UNREADABLE` fallback; the empty-input/short-count digest), which is why R5 seated a lens on it and R5 then found the third. Sharpest single finding: **a many-to-one name sanitiser let a content SWAP between two colliding names produce a byte-identical census** - lost DETECTION, not attribution - fixed with injective percent-encoding. Sharpest process finding: **I refuted the peer's shallow-census objection TWICE on a grep that only covered the plugin directories; measured wider, `.claude/settings.local.json` wires three mode-755 scripts under `.clavity/scratch/` as live SessionStart/SessionEnd hooks**, so the directory the envelope hands the peer really is a code-execution surface. Owner then ruled the census widened, and R4 immediately found the widening's own boundary was a GLOB over an uncontrolled path (a `[` or a trailing slash silently defeated it). **THREE TEST ROWS WERE PROVEN VACUOUS BY MUTANT, one of them written the round before as the fix for a vacuous row** - each confirmed by a mutant that left the suite fully green, then re-confirmed reddening exactly the intended row after the fix. **The citation checker `scripts/check-peer-reply-citations.py` was wired mid-run after I found every brief had claimed `quoted_line` was "checked MECHANICALLY" while I had never checked one - a class-4 False Safety Promise in my own instructions. Run retrospectively over all 8 rounds: **29 of 34 citations verify; the 5 failures are 4 in R2 and 1 in R7** - and in both cases the CLAIM held when I re-derived it, which is the rule: a bad citation does not make a finding wrong.** The peer was asked the stopping question outright at R8 and answered **stop**, declining to manufacture a disagreement after seven rounds of finding one. **STILL OPEN, owner-scoped, NOT closed by this GREEN:** the peer's argument that monitoring `seams/` is theatre while `scratch/` remains an unmonitored write zone a later round could ingest a payload from. |
| 2026-09-01 | `ee2b385..4680613` (Phase 0 - the plugin drift detector, the ROADMAP claims guard, and the self-guarding suite-registration oracle) | 12 | **GREEN - round 12 clean. AWAITING OWNER ADJUDICATION; this row records the run, not a self-granted pass.** Marker `.clavity/agy-marks/agy-capstone.head` = `4680613`, the REVIEWED tip. No review-only breach in the subagent rounds (tree and symbolic ref checked after each); **one breach IS recorded in the peer rounds - see the evidence column.** | **~55 findings folded, 8 rejected by measurement, across 12 rounds.** Briefs `.clavity/seams/capstone-phase0-r{1..8}.md`; rounds 9-12 were dispatched to independent readers. Folds `5499bc4`, `694570a`, `d0f5c9b`, `0ca57f1`, `54d72ef`, `6bd0a01`, `4e152d5`, `51c8351`, `0a6410e`, `6f4d7f1`, `448da79`, `4680613`. 🔴 **THE HEADLINE IS THE BASE RATE: a fold shipped its own new edge in ELEVEN of twelve rounds, and FOUR times a round's fix WAS the next round's finding.** Round 8's `Resolve-Path` fix took the PROVIDER-qualified `.Path`, so a CLEAN install addressed through a PSDrive reported EXTRA and told the operator to hand-delete real payload files - strictly worse than the crash it replaced (round 9). Round 10 then found the same expression's PARAMETER SET was wrong too (`Resolve-Path <path>` binds the WILDCARD set, so both checkers globbed for their own repo root). Round 11 found round 10's summary fix had mis-bucketed PHANTOM - the one outcome check B exists to produce - as "could not be checked". **Treat ~1-in-3 folds as defect-introducing and keep batches small.** 🔴 **THE MOST VALUABLE FINDING ARRIVED ONLY BECAUSE THE PEER DIED.** After round 7 the agy peer returned three error steps to the round, to a re-ask, and to a one-word ping at 719 cascade steps. The owner ruled SUBSTITUTE AN INDEPENDENT READER rather than waive the gate - the discipline buys an INDEPENDENT reader, not that peer's identity. Round 8's substituted readers immediately found that **`actions/checkout` is shallow by default and the guard would have REDDED CI on the first push**: measured on a real `git clone --depth 1`, 43 PHANTOM lines and exit 1 where a full clone exits 0, and none of the ~113 unpushed commits had ever run in CI. They also found check B had **never seen 6 shas that are in the ROADMAP right now** - the `a..b` range form matched nothing. ⚠ **TWO PARALLEL ARMS OVERLAPPED ON THREE FINDINGS**, which contradicts the near-zero-overlap prior in the review-arm experiment (n=7) and should correct it. 🔴 **REVIEW-ONLY BREACH, RECORDED RATHER THAN GLOSSED:** during rounds 5-7 the peer ran `git checkout <sha>`, leaving the repository on a DETACHED HEAD, so the next commit landed off-branch and was invisible to every branch-based check; it was noticed only because the commit output printed `[detached HEAD]`, and recovered by fast-forwarding `main`. Four scratch files were also left in the repository root and deleted. **A file diff does not detect a REF change** - the symbolic ref must be snapshotted before and after EVERY round, which it now is. ⚠ **The peer's `confidence: measured` was wrong again** (round 6): it counted 13 `It` BLOCKS and reported 13 TESTS, where Pester expands data-driven cases to 43 - caught only because the probe carried a control. And a round-7 finding **quoted a line of code that has never existed in the file**, while the claim behind it was correct and worth folding: score the claim and the evidence separately. **Suites at the reviewed tip:** `check-plugin-drift.Tests.ps1` 17/0 (from 8), `check-roadmap-claims.Tests.ps1` 28/0 (from 8), `test-suite-registration.Tests.ps1` 9/0; `check-plugin-drift.ps1` exits 0 against the real repository AND the live install; `check-roadmap-claims.ps1` exits 0 against the real repository ONLY - it takes `-RepoRoot` and `-RoadmapPath`, references no install path anywhere, and parses markdown, so it has NO live-install surface to be checked against. **The original wording here said "both checkers exit 0 against the real repository and the live install", which asserted a verification one of them cannot perform** - caught by the round-4 coverage-gap lens of the Phase 0 DELTA capstone, after four rounds had audited the code and none had audited this prose. **Whole-gate figures are from before the capstone folds and are NOT re-measured at this tip:** fast 425/0 in 544,03s, slow 727/0 in 2699,37s. |
| 2026-09-02 | `4680613..9ef3f83` (Phase 0 DELTA - the four CI-only reds the 12-round capstone never saw, plus this review's own folds) | 6 | **GREEN - round 6 clean, and the STOP was called by the PEER, not by me.** Marker `.clavity/agy-marks/agy-capstone.head` = `9ef3f83`, the REVIEWED tip. 🔴 **THIS ROW EXISTS BECAUSE THE MARKER WAS ONCE WRITTEN WITHOUT A REVIEW.** On 2026-09-01 the owner WAIVED review of this range and directed the marker to `e89d4cb`; it recorded a waiver, not a review. The owner then chose to EARN it instead, and this is that review. A marker is a RECORD, not a PROOF - the only thing that makes this one true is the six rounds below. | **4 findings folded across 6 rounds; 2 verified-but-OUT-OF-RANGE and captured as debt; 3 peer claims refuted by measurement.** Briefs `.clavity/seams/capstone-p0delta-r{1..6}.md`. Folds `7b9f62c` (A1), `a67da2d` (F1), `0bf49d0` (L1+K1), `9ef3f83` (N1). 🔴 **THE SHAPE OF THIS REVIEW: every executable defect was gone by round 2, and every finding after it was in PROSE.** A1 `pub` on a `#[cfg(test)]` const; F1 `#[allow(dead_code)] pub fn read` whose only callers are in `mod tests` - **and F1 WAS A1'S INCOMPLETE FOLD**, round 1 fixed one item and never swept for siblings. Then L1 (rule 4): the Phase 0 ledger row claimed "both checkers exit 0 against the real repository and the live install" when `check-roadmap-claims.ps1` takes only `-RepoRoot`/`-RoadmapPath`, references no install path, and parses markdown - **four rounds had audited the code and none had audited the ledger prose.** Then N1 (rule 4): two of my own commit messages recorded a clippy gate they did not run - `-D warn` with no `--` **exits 1**, and the bare form exits 0 **with no warning denial at all**; cause was padding the commands into an aligned column and truncating them. 🔴 **THE STANDING PATTERN, FIVE DEEP, IS MINE AND IT REACHED THE EVIDENCE ITSELF:** (1) a fold that swept no siblings; (2) "those items are consumed by main.rs" - true of ONE, asserted of a MODULE; (3) no-lib-target generalised into no-visibility-at-all; (4) recording a verification in a form that does not perform it; (5) `1913bdc` says the guard row "cannot be run to completion on this box" - **paired control: default PATH -> WSL bash -> 0P/1F; Git Bash first -> 1P/0F. PATH ORDERING, not the machine.** That one is UNFIXED - the commit is pushed - and is recorded here as its correction. **MEASURED, each with a control in both directions:** the 8.3 test row is non-vacuous (1P/0F -> revert the fix -> **0P/1F RED** -> 1P/0F); its skip guard does not fail open (force the 8.3 probe empty -> Passed=0 Failed=0 **Skipped=1**); `cfg(test)` keeps the dead-code lint live (remove `read()`'s callers -> `error: function read is never used`, exit 101); the path probe is an ORACLE (8 shapes OK, symlink SKIP declared not claimed, and the pre-`41eef75` expression REPRODUCES the CI bug on an 8.3 root). **CARRIED AS DEBT, both verified and both OUT OF RANGE by a range check that was RUN:** four sibling scripts repeat the prefix-arithmetic shape (`check-dangling-consumers`, `check-injected-context`, `check-installer-ascii`, `check-knowledge-store`) with reachability UNPROVEN IN BOTH DIRECTIONS - a short-root run returned OK but `$rel` only prints on failure, so it could not have shown the bug; and three module-internal `pub` consts where the peer was right on the facts and I conceded. Both in `.clavity/local-anomalies.md`. Plus one class-3 from round 6: an empty commit is a poor fix for a false commit message because a reader lands on the original first - `git notes append` is the better mechanism, unused here because notes need explicit fetch/push config. 🔴 **THE PEER CALLED THE STOP, WHICH IS THE POINT OF ASKING IT.** Seat R asked whether the code was sound or whether I was generating prose each round and reviewing my own output; it answered the second, with evidence, and said stop. The driver benefits from stopping and cannot be trusted to call it. ⚠ Its SEAT Q claimed it had "verified" a 71+14 test count it could not have run under review-only - the documented `measured` over-claim, which is why the `cargo fmt --all --check` form was re-checked here independently: **valid, equivalent to `-- --check` (exit 1 on an injected violation, both forms), and the literal command CI runs at `ci-classic.yml:42`.** |


**The two 2026-08-1x STAGE rows were written retroactively on 2026-08-18, and that is a weaker claim than the rest of this table.** Every other row was appended while its capstone was fresh. These two were reconstructed from commit messages after a backwards sweep of the plans found that the two largest reviews on `feature/injected-context-governance` - 20 rounds on the gate, 29 across Stage 2 - had left no row at all, which is exactly the state this ledger's preamble calls indistinguishable from a capstone that never ran. They are recorded because the folds are independently checkable in git; they are LABELLED because a reconstruction must not be laundered into looking contemporaneous. **The lesson is the gap itself: the discipline's own record-keeping step is the one nobody re-runs, and it failed silently on the two biggest reviews in the project's history.**

**A note on the AT-2 row: round 1 returned GREEN with zero findings, and it was wrong.** Its answer walked
the six-item do-not-re-raise ledger it had been given and confirmed each item back to the author. Nothing
in it was a surprise, which is the tell — a review whose findings never surprise the author has agreed
with the author rather than read the code. The defect was on the surface the ledger never mentioned:
`AGY_INBOX_SNAPSHOT_KEEP` fed `tail -n +$((KEEP + 1))` unvalidated, and because bash evaluates a
non-numeric name as 0, a typo (`abc`), a negative, or a literal `0` all became `tail -n +1` — which lists
every slot and deletes them all, **including the snapshot written moments earlier by that same
invocation.** Measured with a control that had to retain: `KEEP=5` → 4 slots kept; `KEEP=0`/`abc`/`-1` →
0 kept, silently, exit 0. A typo in a documented knob was total loss of the history the hook exists to
preserve. Fixed in `3adc045`.

Round 2, run with five rotated lenses and told plainly that round 1 had been refuted by measurement,
produced two findings. One was real (`B1`: the prune re-derived its directory with `!` where the adjacent
line already guards the empty case). **The other arrived with fabricated evidence** — it cited
bullet-bearing comment blocks at "lines 98, 104, 138" of a file that is 62 lines long; those lines do not
exist, and its stated consequence was independently wrong, since the dedup invariant bounds unchanged
content to one slot. The underlying observation was still correct and cheap, so it was folded as
hardening with a mutation-proven test, and the fabrication recorded rather than quietly dropped.

Round 3 rotated five further lenses and returned clean, citing two files it had not been pointed at. Its
highest-risk claim — that the installed layout actually reaches the new hook — was checked independently
rather than accepted: `agy-autotrain/installer/agy-autotrain.iss:53` ships `..\*` with `recursesubdirs`,
excluding only `installer,dist,publish,agy-observations.md`, so a newly added hook file does reach an
installed box. That check is the difference between this GREEN and round 1's.

**This is the second consecutive capstone in which the peer's first-round GREEN was false, and the second
in which a confident, specific, verifiable-looking line citation was invented.** Both times the defect was
found by checking the reviewer rather than by the review. The `rounds` column reads 3; the honest reading
is that the peer produced nothing of value until it was told, with measurements, that it had been wrong.

**A note on the anomaly-capture row, because it is the first entry whose round 1 was rejected.** Round 1
returned GREEN with zero findings on an 864-line diff. It was not accepted: two of its claims were false —
it asserted the two plugins' `SessionStart` registrations were identical (they differ by design, 2 entries
vs 3) and it cited `agy-anomaly-reminder.sh:88-89` for a construct that sits at 70-71. Checking the first
false claim exposed a real defect the round had missed, and the fix for THAT was itself the wrong shape,
which round 2 caught. Round 2 then closed `VERDICT: GREEN` while listing two findings in its own body; that
was scored RED too. Only round 3 was a GREEN whose verdict matched its body.

The `rounds` column therefore counts three, but the honest reading is that **no peer round produced a
finding until the peer was told its previous verdict had been refuted by measurement.** The defects in this
range were found by checking the reviewer, not by the review. That is worth recording precisely because
this ledger is a record and not a proof: a row reading "3 rounds, GREEN" would otherwise imply a
convergence that did not happen the way the number suggests.

---

**Anomaly file drained 2026-08-02 — the 8 captured entries, all fixed rather than filed.** The owner's
ruling was that closing an anomaly means fixing the defect and letting the file empty as a consequence.
`.clavity/local-anomalies.md` is gitignored, so deleting an entry destroys the only record of it; the
dispositions are therefore recorded here.

| # | Anomaly | Disposition |
|---|---------|-------------|
| 1 | consult guard classified any command whose TEXT mentioned the consult CLI as a consult | fixed — classifier anchored on command position (`a25ebd2`'s predecessor `fbd89b9`), integration test seen RED first |
| 2 | consult guard matcher named the MARKETPLACE where the live tool names the PLUGIN, so it never fired on the MCP path | fixed — matcher is now a pattern, plus a namespace assertion that rejects a literal plugin-qualified tool id (`fbd89b9`) |
| 3 | `agy_look` truncates the newest reply out of a long cascade | tracked elsewhere — root cause is the gRPC 4 MB default in `LsChannel.cs`; backlog item `agy-autotrain/docs/fix-the-tool-backlog/grpc-default-max-message-size.md`, committed `1d4a016` |
| 4 | `just test-scripts` grew past the 600s foreground tool cap | fixed — suite partitioned by measured batch runtime (`a52a991`, re-partitioned `0543dcc`) |
| 5 | a dispatched subagent wrote to a file outside the set it was told to touch | fixed — dispatch now states a FILES allow-list and the driver diffs the real change set against it (`9d7f484`). **Corrected by the capstone (`0ca7407`): the original wording said `git status --short`, which is BLIND to a committed write, and implementer subagents commit by default. Both axes are now required.** |
| 6 | seed sync gate used an ALLOW-LIST, so any new shared file was silently ungated | fixed — replaced with discovery over the union of both plugin trees (`29a5db8`) |
| 7 | GROWTH region mojibake-corrupted for 13 days while its sha256 sidecar MATCHED | artifact republished clean; class closed by a tripwire inside `curate-commit`, both binaries (`a25ebd2`) |
| 8 | `agy-curate` had no legal end state for an entry that is neither promotable nor droppable | fixed — HELD added as a fourth disposition, and the promotion rubric's scope bounded (`a51a20e`) |
| 9 | three shipped hooks carried non-ASCII bytes, and the ASCII guard excluded the one file that violated | fixed — bytes stripped and the CLASS closed by `scripts/tests/plugin-hooks-payload.Tests.ps1`, a glob-based guard over the whole payload (`29255bc`). The bytes were comment-only and harmless; the defect was the guard. `agy-consult-guard.Tests.ps1:111` looped ASCII over `-pre.sh`/`-post.sh` and skipped the `-lib.sh` those two SOURCE, and `agy-drive-session-reset.sh` had no suite at all. **The new guard's own first version repeated the bug it exists to catch** — an aggregate count control still passed with one driver's path broken, because the other driver's files satisfied the count; controls are now per-directory and each was proven by breaking each path independently |
| 10 | the pre-drain inbox snapshot silently does not happen when `agy-curate` is invoked as a slash command | tracked — `hooks.json` matches the `Skill` TOOL, which a slash-command invocation never issues, so the guard is absent on the most natural entry path. Backlog item `agy-autotrain/docs/fix-the-tool-backlog/inbox-snapshot-misses-slash-command-path.md` (`77a1d1b`) with two concrete mitigations. Found by RUNNING the drain, not by reading the hook |

The owner's failure criterion for the first triage — "a third outcome appears in practice" — did not trip.

**At the time this section was written the file was NOT empty, and that was the correct outcome** — four
entries captured DURING the fix work remained, and an entry is deleted only when it is genuinely
dispositioned, never to quieten a hook. All four were dispositioned later the same day (see the two
disposition notes at the foot of this file), and only then did the file empty. The order matters: the plan
had predicted a silent hook at exit 0 on the assumption the file held only the original eight. Emptying it
on that schedule would have destroyed the record of four live observations — the exact failure this capture
mechanism exists to prevent — so the hook stayed noisy until each entry had a recorded reason.

**Three of the original eight were found by the capture mechanism during its own construction.**

**On the 2026-08-02 row (`29a5db8..c70f145`, 4 rounds).** Three of the four rounds were scored RED, and
two of those were rounds the peer itself closed GREEN. The pattern is now consistent enough across three
capstones to state as a rule rather than an anecdote: **a clean verdict is a claim, and the claim has to be
checked against its own body and its own citations before it is banked.**

- **Round 1** returned `VERDICT: GREEN` while its Lens 1 enumerated real evasions and a real false
  positive. It also carried three false citations, two labelled MEASURED — including a classifier located
  at `lib.sh:143-159` in a 103-line file, which the same paragraph had cited correctly as `:66` two lines
  earlier. Every finding it listed turned out to be REAL and reproducible; the disposition was what failed.
- **Round 3** returned `VERDICT: GREEN` with "no new findings" in all six lenses, while its Lens 1 text
  described a regression that the round-2 fold had introduced. Measured: the original code left two body
  lines alone, the round-2 replacement deleted both.
- **Round 2** was the honest RED, and all four of its findings were real. Its verdict matched its body.

**Verify the FIX, not only the finding — proven twice in this one capstone, from both directions.** In
round 1 the peer's proposed regex was correct about the defect and harmful in its remedy: measured
one-for-one, it bought one evasion and cost one new false alarm, so it was refused and a narrower variant
shipped. In round 3 the harmful fix was MINE — a correct fix for a repeating sed range that quietly made
the no-frontmatter case worse. The same single line needed three attempts, and the first two each broke an
adjacent case.

**Severity was bounded by a fact found during review, not assumed before it:** the consult guard's MCP
path is classified structurally by tool name and returns before the shell regex is ever reached, so the
primary consult channel was never exposed to any of the classifier findings.

**The most reachable defect was in the verification step of a mechanism this same range had just shipped.**
The dispatch allow-list told the driver to check `git status --short`; measured, that command prints
nothing after a subagent commits a file outside its list, and implementer subagents commit by default. The
mechanism's own commit message says a list without a diff is theatre. It prescribed the wrong diff.

---

**Anomaly #1 (`.git/index` race) dispositioned 2026-08-02 — DELETED, suites exonerated by construction.**

Captured 2026-08-01: `fatal: .git/index: index file smaller than expected`, seen twice on stderr during a
per-file runtime measurement, after `abort-drain.Tests.ps1` and after `docs-audit.Tests.ps1`. The cause was
recorded as UNCONFIRMED at capture time, because the driver's own concurrent `git status` calls also write
`.git/index` — two candidate authors, and no basis to blame either.

**Settled from the CODE, which three attempted reproductions could not have settled.** MEASURED by audit:

- `scripts/tests/docs-audit.Tests.ps1`, `scripts/docs-audit.ps1` and `scripts/docs-audit-lib.ps1` contain
  **zero `git` invocations** between them; the single textual match is a comment at
  `docs-audit.Tests.ps1:555`. That suite is structurally incapable of touching any index.
- `scripts/tests/abort-drain.Tests.ps1` creates its repo under `[System.IO.Path]::GetTempPath()` (`:8`),
  and **every** git call in the file sits inside a `Push-Location $script:Repo` / `Pop-Location` pair —
  `:10-15`, `:34-37`, `:60-63`, `:99-101`, and `:105` inline. Zero unscoped calls.

Neither suite can reach the real repository's `.git/index`. The correlation with those two files is a
sampling artifact: at 261.3s and 120.6s they are the two longest in the partition and together span most
of a full run, so any ambient collision is overwhelmingly likely to land inside their window.

**The reproduction attempts are recorded because a negative result is worth keeping, not because they
decided it.** Three conditions, all clean: quiet (185.75s), with a `git status --porcelain` loop hammering
the index (271.9s), and the same with the loop's own stderr captured (347.99s, 0 bytes of stderr). Runtime
rising monotonically is the positive control that the contention was real rather than a loop that never
started. The capture path was separately proven to carry a git `fatal:` through `pwsh` into the log, so
these are real negatives and not empty-vs-empty passes.

**What this does and does not establish.** It establishes that no code in this repository caused it, which
is what makes it undeployable as a backlog item — there is no change here that would fix it. It does NOT
establish what did. The driver's own concurrent `git status` remains the leading candidate and was
demonstrably running at the time; an external Windows filter driver is another. That mechanism is INFERRED
and unproven, and is recorded as such rather than asserted.

**Durable driving rule, which is the part worth keeping:** do not poll git while a test suite is running.
The reader is the victim of a torn index, not its author, so a supervising `git status` can surface an
error it did not cause — and then be mistaken for evidence against the code under test.

**The remaining three anomalies, dispositioned 2026-08-02 — all DELETED, none a defect in this repo.**
Triaged by hand against the `open-issues` rule (promote with an owner, or delete with a recorded reason;
no parked state), because that skill is not yet installed on this machine. The anomaly file is gitignored,
so the reasons live here.

**1. "a Pester suite mutates the TRACKED `build/members.json`."** DELETED — the same misattribution as the
`.git/index` entry above, and found the same way. MEASURED: every `members.json` write in the suites
targets a fixture, not the repository. `check-plugin-namespace.Tests.ps1:14,54` writes under
`$env:TEMP/ns-clean-<pid>-<rand>` (`:7`); `check-member-docs.Tests.ps1:166` writes under
`$TestDrive/<guid>/build/` (`:157-158`); `check-roster.Tests.ps1:10` only READS the real file and sends
its own writes to `$TestDrive` (`:17,25,37`). No suite writes the tracked file. The emitter is a git
WRITE-op notice: that identical warning printed on many `git add` calls made by the driver during this
very session, and the driver was running git write-ops during the original measurement.

**2. "a `\uXXXX` escape written through the editing tool can arrive DECODED."** DELETED from this backlog,
and the observation kept as a driving rule instead. It is REAL and was reproduced twice — a subagent hit
it inserting mojibake signatures into two C# files, and the driver reproduced it deliberately (`A`
produced a bare `0x41`, confirmed by `od -c`). But the mechanism sits in the agent's editing harness, not
in this codebase: there is no change here that would fix it, so carrying it as a repo backlog item would
imply an owner who does not exist. The impact is worth restating because it is subtle — source that must
contain escape TEXT (mojibake signatures, regex character classes, test fixtures) can silently become
decoded non-ASCII that still compiles and still passes its tests, which is precisely the class the
`curate-commit` tripwire in this range exists to catch, arriving through the authoring path instead of the
transport. **Rule: after writing any escape literal, verify at byte level.**

**3. "`git commit -m` shell-expands its own message."** DELETED — not a defect at all, but correct and
documented shell behaviour, hit while writing a commit message that DOCUMENTED shell syntax. The
substitution executed, printed `command not found`, and the message committed as `X=.` with the documented
text gone. The commit SUCCEEDED, which is what made it silent; the corruption was visible only by reading
the message back, and cost an `--amend`. **Rule: write commit messages via a quoted-delimiter heredoc into
`git commit -F -` whenever the message contains `$( )`, backticks, or `${ }`.**

**The pattern across all four dispositioned entries is the finding.** Two of the original "suite defects"
were the driver's own tooling, misattributed to whatever happened to be running at the time, and a third
was standard shell semantics. Each was captured honestly — the `.git/index` entry explicitly recorded its
cause as UNCONFIRMED — but each would have been promoted as a code defect on the strength of correlation
alone. What settled all of them was reading the code, not re-running the scenario. **Correlation with a
long-running suite is not evidence about that suite; it is evidence about which window was widest.**

---

## Isolation probes - NOT capstones

**These rows are deliberately kept out of the table above, whose header says "One row per capstone."** An
isolation probe answers "is this mechanism the one doing the work?", which is a different and much weaker
claim than a capstone's. Filing one as a capstone row would quietly make the table mean two things.

### 2026-09-02 - does the anti-wrap-up clause isolate as the mechanism? **NO. NEGATIVE RESULT.**

**What was asked.** ROADMAP work on the peer reply contract rested on a clause that was strongly
SUPPORTED as the mechanism but never ISOLATED: the rounds whose reports were displaced by a closing
pleasantry differed from the successful probes in more than that one sentence. Phase 1 Task 5 exists to
run the missing comparison.

**Design.** Two arms, `.clavity/seams/task5-isolation-arm-with.md` and `...-arm-without.md`. Arm B was
generated from arm A by deleting exactly one paragraph in code, with an assertion that the deletion
applied; `diff -u` between the two briefs is **a single hunk, four lines plus its blank**, and nothing
else differs - including the ask-don't-guess line that closes both. Both briefs demanded a 4-row census,
findings, a `[VERIFIED: ...]` block, **closing disclosures**, and a terminal token, in that order. The
closing-disclosure requirement is the point: it manufactures the exact ordering conflict the clause exists
to resolve. Both arms omitted `discipline` and `artifactPath` on `agy_ask`, so the driver's 13b echo check
could not add a second required-block-before-the-token and confound the measurement.

**Order was reversed from the plan's, deliberately.** The plan lists WITH first. Contamination is
asymmetric: a peer that has just been shown the rule and then answers correctly without it proves
nothing, whereas the control run on a peer that has never seen the rule is clean either way it falls. So
the control ran first, and it is the arm that carries the result.

**Arm B - WITHOUT the clause (the control, and the informative arm).** `AnswerTruncated: false`,
`TerminalTokenMissing: false`. **The full body arrived inline**: the 4-row census, two findings with
dispositions and severity classes, the `[VERIFIED: ...]` block, both closing disclosures, and
`[VERDICT: FINDINGS]` as the last thing in the reply. ~40 trajectory steps, i.e. it really did open the
files. **Nothing was displaced, and the clause was absent.**

**Arm A - WITH the clause. Contaminated, contributes nothing.** The answer came back essentially
character-identical to arm B's in **~6 trajectory steps** against arm B's ~40 - the peer replayed its
cached analysis rather than re-reading anything. This is the repetition confound that "send the same brief
twice" makes unavoidable, and it was predicted before the arm ran.

**Conclusion, stated as the plan requires.** **The clause is NOT isolated as the mechanism.** One
uncontaminated arm, n=1, produced a complete inline body with the token last while the clause was absent.
That is a datum against the clause being NECESSARY in this condition; it is not a refutation of its value,
and it is not a licence to remove it. **The honest status is unchanged from before this probe:
SUPPORTED, NOT ISOLATED** - and now with one negative datum on the record.

**What this probe does NOT settle, stated rather than hidden.** This brief differs from the historically
failing rounds in more than the clause: a different seat, a different deliverable shape, no 13b
parameters, and a peer in a different conversation state. A single non-displacing control under those
conditions cannot show that displacement never happens without the clause - only that it did not happen
here. Two arms is not a sample.

**Byproduct, and the more valuable half of the run.** The probe was pointed at real code - `d33416c..a72f618`,
which had had no review - and it surfaced a genuine defect in that commit: the clause shipped as unmarked
driver-facing prose although it is written in the peer's second person, in files where `> ` marks payload
text 4 times out of 4. In `agy-capstone` that misreading contradicts `SKILL.md:213`. Folded at `62be727`
with a passing AND a failing control. The peer's stated MECHANISM was rejected by measurement (it
described a payload generator that does not exist, and its own closing disclosure flagged that assumption
as unverified), and two of its four census rows were rejected as reply-vs-report conflations. **A probe
designed to measure one thing paid for itself by finding another** - which is an argument for pointing
probes at live code rather than at fixtures.

**Envelope.** Clean in both arms: `git status --short` unchanged (only the owner's untracked `TODO.md`),
`HEAD` and the reflog tip unmoved at `97bb44b`, and the sanctioned scratch dir
`.clavity/scratch/task5-isolation/` still empty. No review-only breach.
