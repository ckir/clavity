# Package manifests and marketplace metadata - read by the plugin loader, never by a model.
**/plugin.json
**/.claude-plugin/**
**/.mcp.json
# Human-facing repository documentation - shipped, but not loaded into an agent's context.
**/README.md
**/NOTICE
**/LICENSE
# Build and packaging inputs, not instruction text.
**/installer/**
# ANCHORED, and the anchoring is the point. These were once `**/dist/**` and `**/publish/**`, matching a
# directory of that name at ANY depth - which meant a skill, hook or knowledge file placed in a folder
# named `dist` was subtracted from the audit and shipped unchecked. MEASURED: five files, one per shipped
# class, each carrying a real em dash, all invisible to the gate. A subtraction that matches by NAME
# rather than by PLACE is an open door; these name the real build directories instead. There are only
# seven such directories inside all nine domain roots, so this list is short by measurement, not by luck.
agy-autotrain/dist/**
commonmemory/dist/**
clavity-classic/publish/**
clavity-dotnet/publish/**
clavity-classic/agy-mcp-bridge/.venv/**
clavity-classic/agy-mcp-bridge/__pycache__/**
agy-autotrain/docs/fix-the-tool-backlog/.ruff_cache/**
# A .git DIRECTORY inside a domain root is VCS plumbing, like the .git FILE below.
**/.git/**
# Owner ruling 2026-08-09. These ship to a user's machine through the installers' recursive wildcard, so
# the mojibake half of the encoding rationale technically reaches them - but the DOMAIN is "what enters an
# agent's context", and neither of these ever does. A .gitignore is consumed by git and a tmux.conf by the
# psmux session; no agent reads either. Auditing them was discovery drift, not coverage.
**/.gitignore
**/tmux.conf
# Owner ruling 2026-08-09, paired with adding clavity-classic/agy-mcp-bridge as a domain root. That root
# exists for ONE injected file - its SKILL.md, a headless sub-agent's system prompt - and carries a Python
# MCP server alongside it. THIS rule is scoped correctly - every .py in the whole domain lives under that
# one root - but its original reason was stated too broadly and that generalisation is FALSE: "source code
# is never injected into an agent's context" does not hold. Counter-example, measured 2026-08-11:
# ghidrust/crates/ghidrust-mcp/src/tools.rs holds 19 `pub const DESC_*` blocks totalling roughly 12 KB of
# description text that MCP delivers to EVERY agent via tools/list. The correct reason for THIS rule is narrower: the .py under
# agy-mcp-bridge is a transport implementation whose text no agent reads. Judge each case on whether the
# text reaches an agent, never on the file being "source code".
#
# WHERE THE BOUNDARY FALLS, and why tools.rs is not added as a domain entry. The encoding invariant exists
# for the Inno / CP437 route, where a mojibake byte corrupts a real installer. MCP tool descriptions travel
# UTF-8 JSON-RPC over stdio, where UTF-8 is well-defined, so that invariant categorically does not apply
# and auditing tools.rs for it would red-gate correct content (it carries 32 non-ASCII characters today
# - 25 em dashes, 4 section signs, 2 arrows, 1 ellipsis - all legitimate). Its ACCURACY is hand-verified instead, dated, with a named re-check trigger - see
# docs/coverage-debt.md.
**/*.py
**/.env.example
# A git WORKTREE marks its checkout with a .git FILE (contents: "gitdir: <path>"), not a directory, and
# the prune only removes .git as a DIRECTORY - deliberately, since requiring a trailing separator is what
# stops a file merely NAMED dist or bin being dropped. So the marker file lands in the corpus and takes
# every invariant. MEASURED 2026-08-09: a .git file carrying a BOM produced a real encoding violation. It
# is git plumbing, not injected context, so it is subtracted here rather than pruned.
**/.git
# ...and the CHECKOUT THAT MARKER SITS IN. Subtracting the marker file was only half the job: a worktree
# is a COMPLETE SECOND COPY of the repository, so every domain root, skill and hook exists twice on disk.
# MEASURED 2026-08-10, capstone round 17: creating a worktree at the location this repository's own
# worktree skill defaults to turned the suite RED - three rows in 'domain coverage', which walk the
# FILESYSTEM rather than git, reported every SKILL.md in the second checkout as "outside every domain root
# and audited by nothing". They were duplicates of files already audited in the primary tree.
#
# THE SUPPORTED WORKFLOW WAS THE TRIGGER, which is what makes this worth subtracting rather than
# documenting: the skill creates worktrees here by default, and git only ignores the directory through
# .git/info/exclude - a LOCAL, UNCOMMITTED file - so a fresh clone does not ignore it at all.
#
# The gate's own corpus never included these (it walks domain roots, and this is not under one), so this
# changes no violation. It is the coverage QUERY, which walks the whole repository, that needed telling.
.worktrees/**
