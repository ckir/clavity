# Copy this file to `.env` (same dir) and fill in your key.
# clavity loads `.env` at startup; a value set in your real shell environment wins.
# A user-level fallback is also read from ~/.clavity/.env
GEMINI_API_KEY=
