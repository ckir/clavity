[project]
name = "clavity"
version = "0.1.0"
description = "Claude remote-controls a live Antigravity (agy) session in the same folder, via a psmux doorbell + the agentmemory signal bus."
requires-python = ">=3.10"
dependencies = []

[dependency-groups]
dev = [
    "ruff>=0.15.16",
    "pytest>=8.0.0",
]
