//! Packager (STUB / skeleton): build binary, generate both hosts' files,
//! assemble dist/<plugin>/ (universal mode) or dist/<plugin>-{claude,agy}/ (split).
//!
//! TODO(first-plugin): Mode enum + package().
